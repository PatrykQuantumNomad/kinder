---
phase: 39-docker-and-tool-configuration-checks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - pkg/internal/doctor/disk.go
  - pkg/internal/doctor/disk_unix.go
  - pkg/internal/doctor/disk_other.go
  - pkg/internal/doctor/disk_test.go
  - pkg/internal/doctor/daemon.go
  - pkg/internal/doctor/daemon_test.go
  - pkg/internal/doctor/snap.go
  - pkg/internal/doctor/snap_test.go
  - pkg/internal/doctor/check.go
autonomous: true
requirements: [DOCK-01, DOCK-02, DOCK-03]

must_haves:
  truths:
    - "kinder doctor warns when disk space is below 5GB and fails when below 2GB"
    - "kinder doctor detects init:true in daemon.json across 6+ candidate locations"
    - "kinder doctor detects Docker installed via snap and warns about TMPDIR"
  artifacts:
    - path: "pkg/internal/doctor/disk.go"
      provides: "diskSpaceCheck struct with readDiskFree and execCmd deps"
      contains: "diskSpaceCheck"
    - path: "pkg/internal/doctor/disk_unix.go"
      provides: "statfsFreeBytes implementation using unix.Statfs for linux+darwin"
      contains: "statfsFreeBytes"
    - path: "pkg/internal/doctor/disk_other.go"
      provides: "statfsFreeBytes stub for non-unix platforms"
      contains: "statfsFreeBytes"
    - path: "pkg/internal/doctor/disk_test.go"
      provides: "Tests for disk space thresholds: ok >=5GB, warn <5GB, fail <2GB"
      contains: "TestDiskSpaceCheck"
    - path: "pkg/internal/doctor/daemon.go"
      provides: "daemonJSONCheck with readFile and homeDir deps, 6+ candidate paths"
      contains: "daemonJSONCheck"
    - path: "pkg/internal/doctor/daemon_test.go"
      provides: "Tests for daemon.json multi-path resolution and init:true detection"
      contains: "TestDaemonJSONCheck"
    - path: "pkg/internal/doctor/snap.go"
      provides: "dockerSnapCheck with lookPath and evalSymlinks deps"
      contains: "dockerSnapCheck"
    - path: "pkg/internal/doctor/snap_test.go"
      provides: "Tests for snap symlink detection"
      contains: "TestDockerSnapCheck"
  key_links:
    - from: "pkg/internal/doctor/disk.go"
      to: "pkg/internal/doctor/disk_unix.go"
      via: "statfsFreeBytes function reference"
      pattern: "statfsFreeBytes"
    - from: "pkg/internal/doctor/check.go"
      to: "pkg/internal/doctor/disk.go"
      via: "newDiskSpaceCheck() in allChecks registry"
      pattern: "newDiskSpaceCheck"
    - from: "pkg/internal/doctor/check.go"
      to: "pkg/internal/doctor/daemon.go"
      via: "newDaemonJSONCheck() in allChecks registry"
      pattern: "newDaemonJSONCheck"
    - from: "pkg/internal/doctor/check.go"
      to: "pkg/internal/doctor/snap.go"
      via: "newDockerSnapCheck() in allChecks registry"
      pattern: "newDockerSnapCheck"
---

<objective>
Implement disk space, daemon.json, and Docker snap checks for kinder doctor.

Purpose: These three checks address the most common Docker configuration problems: insufficient disk space causing silent kubelet eviction, daemon.json "init: true" causing cryptic container failures, and snap-installed Docker breaking TMPDIR access. All three belong to the "Docker" category.
Output: 8 new source/test files, updated check.go registry, promoted golang.org/x/sys in go.mod
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-docker-and-tool-configuration-checks/39-RESEARCH.md
@.planning/phases/38-check-infrastructure-and-interface/38-01-SUMMARY.md
@.planning/phases/38-check-infrastructure-and-interface/38-02-SUMMARY.md

@pkg/internal/doctor/check.go
@pkg/internal/doctor/runtime.go
@pkg/internal/doctor/tools.go
@pkg/internal/doctor/testhelpers_test.go
@pkg/internal/doctor/runtime_test.go

<interfaces>
<!-- Key types and contracts from Phase 38. Executor should use these directly. -->

From pkg/internal/doctor/check.go:
```go
type Check interface {
    Name() string
    Category() string
    Platforms() []string  // nil = all platforms; []string{"linux"} = linux-only
    Run() []Result
}

type Result struct {
    Name     string `json:"name"`
    Category string `json:"category"`
    Status   string `json:"status"`           // "ok", "warn", "fail", "skip"
    Message  string `json:"message,omitempty"`
    Reason   string `json:"reason,omitempty"`
    Fix      string `json:"fix,omitempty"`
}

var allChecks = []Check{
    newContainerRuntimeCheck(),
    newKubectlCheck(),
    newNvidiaDriverCheck(),
    newNvidiaContainerToolkitCheck(),
    newNvidiaDockerRuntimeCheck(),
}
```

From pkg/internal/doctor/runtime.go (deps struct pattern to follow):
```go
type containerRuntimeCheck struct {
    lookPath func(string) (string, error)
    execCmd  func(name string, args ...string) exec.Cmd
}

func newContainerRuntimeCheck() Check {
    return &containerRuntimeCheck{
        lookPath: osexec.LookPath,
        execCmd:  exec.Command,
    }
}
```

From pkg/internal/doctor/testhelpers_test.go (test doubles to reuse):
```go
func newFakeExecCmd(results map[string]fakeExecResult) func(name string, args ...string) exec.Cmd
type fakeExecResult struct {
    lines string
    err   error
}
```

From pkg/internal/version/version.go (available for version parsing):
```go
func ParseSemantic(str string) (*Version, error)
func (v *Version) Minor() uint
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement disk space check with build-tagged Statfs helper</name>
  <files>go.mod, go.sum, pkg/internal/doctor/disk.go, pkg/internal/doctor/disk_unix.go, pkg/internal/doctor/disk_other.go, pkg/internal/doctor/disk_test.go</files>
  <behavior>
    - Test: readDiskFree returns 10GB -> status "ok", message contains "GB free"
    - Test: readDiskFree returns 4GB -> status "warn", message contains "4.0 GB free", reason mentions "Low disk space"
    - Test: readDiskFree returns 1GB -> status "fail", message contains "1.0 GB free", reason mentions "Insufficient"
    - Test: readDiskFree errors AND fallback to "/" also errors -> status "warn", message "Could not check disk space"
    - Test: docker info returns custom data root path -> readDiskFree called with that path
    - Test: docker info fails -> falls back to default path, then readDiskFree on that or "/"
  </behavior>
  <action>
1. Promote golang.org/x/sys from indirect to direct in go.mod by adding it to the main require block (it is already at v0.41.0). Run `go mod tidy` to update go.sum.

2. Create `pkg/internal/doctor/disk_unix.go` with build tag `//go:build linux || darwin`:
   - Import `golang.org/x/sys/unix`
   - Implement `statfsFreeBytes(path string) (uint64, error)` that calls `unix.Statfs(path, &stat)` and returns `stat.Bavail * uint64(int64(stat.Bsize))` -- the int64 cast handles macOS uint32 Bsize vs Linux int64 Bsize

3. Create `pkg/internal/doctor/disk_other.go` with build tag `//go:build !linux && !darwin`:
   - Return `0, errors.New("disk space check not supported on this platform")`

4. Create `pkg/internal/doctor/disk.go`:
   - Define `diskSpaceCheck` struct with `readDiskFree func(path string) (uint64, error)` and `execCmd func(name string, args ...string) exec.Cmd` fields
   - `newDiskSpaceCheck()` wires `statfsFreeBytes` and `exec.Command`
   - Name: "disk-space", Category: "Docker", Platforms: nil (all platforms -- the build tag handles unsupported platforms via the readDiskFree error path)
   - Run() logic: First get Docker data root via `docker info --format '{{.DockerRootDir}}'` using execCmd. If that fails, use "/var/lib/docker" as default. Call readDiskFree on that path. If it fails, fall back to readDiskFree("/"). If that also fails, return warn "Could not check disk space". Otherwise compute freeGB as float64(bytes) / (1024*1024*1024). Return fail if <2.0, warn if <5.0, ok otherwise. Include the data root path in the ok message.
   - Fix message for fail: "Free disk space: docker system prune && docker image prune -a"
   - Fix message for warn: "Free disk space: docker system prune"

5. Create `pkg/internal/doctor/disk_test.go` with table-driven tests:
   - Use `t.Parallel()` on both the top-level test and each subtest
   - Inject fake `readDiskFree` directly on the struct (no need for fakeCmd for simple function deps)
   - Use `newFakeExecCmd` for the docker info command
   - Test all threshold cases: ok (>=5GB), warn (<5GB but >=2GB), fail (<2GB)
   - Test docker info returning custom path, docker info failing (fallback behavior)
   - Test readDiskFree erroring on data root but succeeding on "/"
   - Test both readDiskFree calls failing -> warn result

Do NOT register the check in allChecks yet -- that happens in Task 2 alongside the other two checks.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && go vet ./... && go test ./pkg/internal/doctor/... -run TestDiskSpaceCheck -v -count=1</automated>
  </verify>
  <done>disk.go, disk_unix.go, disk_other.go, disk_test.go all exist. go.mod has golang.org/x/sys as direct dependency. All disk space threshold tests pass. Project compiles on the host platform.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement daemon.json and Docker snap checks, register all three in allChecks</name>
  <files>pkg/internal/doctor/daemon.go, pkg/internal/doctor/daemon_test.go, pkg/internal/doctor/snap.go, pkg/internal/doctor/snap_test.go, pkg/internal/doctor/check.go</files>
  <behavior>
    - daemon.json: no file at any candidate -> status "ok", message "No daemon.json found"
    - daemon.json: file with {"init": true} -> status "warn", message contains "init", fix mentions "Remove"
    - daemon.json: file with {"init": false} -> status "ok"
    - daemon.json: file with {"storage-driver": "overlay2"} (no init field) -> status "ok"
    - daemon.json: malformed JSON -> status "warn", message contains "not valid JSON"
    - daemon.json: multiple candidates, first unreadable, second has init:true -> detects it
    - snap: docker not found -> status "skip"
    - snap: resolved path contains "/snap/" -> status "warn", message mentions "snap", fix mentions "TMPDIR"
    - snap: resolved path does not contain "/snap/" -> status "ok"
    - snap: evalSymlinks fails -> falls back to original path check
    - allChecks registry contains all 10 checks in correct category order after registration
  </behavior>
  <action>
1. Create `pkg/internal/doctor/daemon.go`:
   - Define `daemonJSONCheck` struct with `readFile func(string) ([]byte, error)`, `homeDir func() (string, error)`, and `goos string` fields
   - `newDaemonJSONCheck()` wires `os.ReadFile`, `os.UserHomeDir`, and `runtime.GOOS`
   - Name: "daemon-json-init", Category: "Docker", Platforms: nil (all platforms -- daemon.json exists on all Docker platforms)
   - Implement `daemonJSONCandidates() []string` that builds the prioritized list:
     1. `/etc/docker/daemon.json` (native Linux)
     2. `$HOME/.docker/daemon.json` (Docker Desktop macOS + Rancher Desktop)
     3. `$XDG_CONFIG_HOME/docker/daemon.json` or `$HOME/.config/docker/daemon.json` (rootless Docker)
     4. `/var/snap/docker/current/config/daemon.json` (snap Docker)
     5. `$HOME/.rd/docker/daemon.json` (Rancher Desktop alternate)
     6. `$ProgramData\docker\config\daemon.json` (Windows, only when goos=="windows")
   - Run() iterates candidates. For each: try readFile; if error, continue to next. Parse JSON into `map[string]interface{}`. If parse fails, warn "daemon.json at {path} is not valid JSON". If parsed and has `"init": true` (check bool type assertion), warn with reason "Docker init=true breaks kind nodes with: Couldn't find an alternative telinit implementation to spawn" and fix "Remove \"init\": true from {path} and restart Docker". If parsed and no init:true, return ok "daemon.json checked ({path})". If no candidate readable, return ok "No daemon.json found (Docker using defaults)".
   - IMPORTANT: File absence is OK (not an error). Only warn if init:true found or JSON malformed.

2. Create `pkg/internal/doctor/daemon_test.go` with table-driven tests:
   - Inject fake `readFile` that maps paths to content (like the research example)
   - Inject fake `homeDir` returning "/home/testuser"
   - Set `goos` to "linux" for most tests
   - Test cases: no files exist (ok), init:true found (warn), init:false (ok), no init field (ok), malformed JSON (warn), second candidate has init:true (first returns os.ErrNotExist), Windows path included when goos="windows"

3. Create `pkg/internal/doctor/snap.go`:
   - Define `dockerSnapCheck` struct with `lookPath func(string) (string, error)` and `evalSymlinks func(string) (string, error)` fields
   - `newDockerSnapCheck()` wires `osexec.LookPath` and `filepath.EvalSymlinks`
   - Name: "docker-snap", Category: "Docker", Platforms: `[]string{"linux"}` (snap is Linux-only)
   - Run(): If lookPath("docker") fails, return skip "(docker not found)". Resolve path via evalSymlinks. If evalSymlinks fails, use the original path. If resolved path contains "/snap/", warn "Docker installed via snap ({resolved})" with reason about TMPDIR and fix "Set TMPDIR to a snap-accessible directory: export TMPDIR=$HOME/tmp && mkdir -p $TMPDIR". Otherwise ok "Docker not installed via snap".
   - Use `osexec` import alias for `os/exec` (same as tools.go pattern) to avoid conflict with `sigs.k8s.io/kind/pkg/exec`.

4. Create `pkg/internal/doctor/snap_test.go` with table-driven tests:
   - Inject fake lookPath and evalSymlinks directly on struct
   - Test: docker not found -> skip
   - Test: resolved to /snap/bin/docker -> warn
   - Test: resolved to /usr/bin/docker -> ok
   - Test: evalSymlinks error -> falls back to lookPath result, checks for /snap/

5. Update `pkg/internal/doctor/check.go`:
   - Reorder allChecks to group by category: Runtime, Docker (new), Tools, GPU
   - New allChecks order:
     ```go
     var allChecks = []Check{
         // Category: Runtime
         newContainerRuntimeCheck(),
         // Category: Docker
         newDiskSpaceCheck(),
         newDaemonJSONCheck(),
         newDockerSnapCheck(),
         // Category: Tools
         newKubectlCheck(),
         // Category: GPU
         newNvidiaDriverCheck(),
         newNvidiaContainerToolkitCheck(),
         newNvidiaDockerRuntimeCheck(),
     }
     ```
   - This groups the 3 new Docker checks together between Runtime and Tools
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && go vet ./... && go test ./pkg/internal/doctor/... -v -count=1</automated>
  </verify>
  <done>daemon.go, daemon_test.go, snap.go, snap_test.go all exist. check.go updated with 8 checks in Runtime/Docker/Tools/GPU category order. All doctor package tests pass including disk, daemon, snap, and all existing checks. go build and go vet clean.</done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles successfully (verifies build tags, imports, go.mod)
2. `go vet ./...` reports no issues
3. `go test ./pkg/internal/doctor/... -v -count=1` -- all tests pass for all checks
4. `go test ./... -count=1` -- full test suite passes (no regressions)
5. Verify allChecks contains exactly 8 entries with categories: Runtime, Docker, Docker, Docker, Tools, GPU, GPU, GPU
</verification>

<success_criteria>
- diskSpaceCheck warns at <5GB and fails at <2GB with correct messages and fix commands
- daemonJSONCheck searches 6+ candidate paths and only warns on init:true or malformed JSON
- dockerSnapCheck resolves symlinks and detects /snap/ path prefix
- All three checks follow the deps struct pattern with injectable dependencies
- All three checks have table-driven parallel tests with injected fakes
- golang.org/x/sys promoted from indirect to direct in go.mod
- go build, go vet, and full test suite all pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-docker-and-tool-configuration-checks/39-01-SUMMARY.md`
</output>
