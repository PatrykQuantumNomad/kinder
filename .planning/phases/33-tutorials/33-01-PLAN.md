---
phase: 33-tutorials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kinder-site/src/content/docs/guides/tls-web-app.md
autonomous: true
requirements:
  - GUIDE-01

must_haves:
  truths:
    - "TLS tutorial guides user from cluster creation through pushing an image to the local registry, exposing it via Envoy Gateway with a cert-manager TLS certificate, and verifying HTTPS works"
    - "Tutorial has clearly marked prerequisites, copy-pasteable commands, and expected output after every key command"
    - "macOS/Windows users have a working port-forward alternative for accessing the gateway"
  artifacts:
    - path: "kinder-site/src/content/docs/guides/tls-web-app.md"
      provides: "Complete TLS web app tutorial replacing placeholder"
      min_lines: 150
      contains: "ClusterIssuer"
  key_links:
    - from: "tls-web-app.md"
      to: "cert-manager addon"
      via: "selfsigned-issuer ClusterIssuer reference"
      pattern: "kind: ClusterIssuer"
    - from: "tls-web-app.md"
      to: "envoy-gateway addon"
      via: "gatewayClassName: eg"
      pattern: "gatewayClassName: eg"
    - from: "tls-web-app.md"
      to: "local-registry addon"
      via: "localhost:5001 push/pull"
      pattern: "localhost:5001"
    - from: "tls-web-app.md"
      to: "metallb addon"
      via: "LoadBalancer IP for gateway"
      pattern: "curl -k https"
---

<objective>
Write the complete TLS web app tutorial that guides users through deploying a TLS-secured web application using four kinder addons working together: Local Registry, Envoy Gateway, cert-manager, and MetalLB.

Purpose: This is the showpiece "batteries included" tutorial demonstrating how kinder's pre-installed addons compose into a real workflow. It replaces the existing `:::note[Coming soon]` placeholder.
Output: Complete `tls-web-app.md` with prerequisites, step-by-step commands, YAML manifests, expected output blocks, platform-specific notes, and a clean up section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tutorials/33-RESEARCH.md
@kinder-site/src/content/docs/guides/tls-web-app.md
@kinder-site/src/content/docs/addons/cert-manager.md
@kinder-site/src/content/docs/addons/envoy-gateway.md
@kinder-site/src/content/docs/addons/metallb.md
@kinder-site/src/content/docs/addons/local-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete TLS web app tutorial</name>
  <files>kinder-site/src/content/docs/guides/tls-web-app.md</files>
  <action>
Replace the `:::note[Coming soon]` placeholder with the full TLS web app tutorial. Keep the existing frontmatter (title, description). Follow the tutorial page structure from 33-RESEARCH.md Pattern 1.

The tutorial must follow this exact flow with these sections:

1. **Opening paragraph** -- One paragraph explaining what the user will build (a TLS-secured nginx app) and which addons are involved (Local Registry, Envoy Gateway, cert-manager, MetalLB). Mention all four by name.

2. **## Prerequisites** -- Bullet list:
   - kinder installed (link to `/installation`)
   - Docker (or Podman) installed and running
   - `kubectl` installed and on PATH
   - `curl` installed (for HTTPS verification with `-k` flag)

3. **## Step 1: Create the cluster** -- `kinder create cluster` command. Add a `:::tip` noting all four required addons are enabled by default. Show expected output snippet (the addon timing summary).

4. **## Step 2: Verify addons are ready** -- Show commands to check cert-manager pods (3 Running), envoy-gateway-system pods (Running), metallb-system pods (Running), and local registry container (`docker ps --filter name=kind-registry`). Include expected output blocks for each. Add a `:::note` about cert-manager webhook taking 30-60 seconds.

5. **## Step 3: Push an image to the local registry** -- Pull `nginx:alpine`, tag as `localhost:5001/myapp:v1`, push. Show expected output with digest.

6. **## Step 4: Deploy the application** -- Full Deployment + Service YAML in a single `kubectl apply -f - <<EOF` block. Deployment uses `image: localhost:5001/myapp:v1`. Service on port 80 targeting port 80. Then `kubectl get pods` with expected output showing Running.

7. **## Step 5: Create a TLS certificate** -- Certificate YAML using `issuerRef.name: selfsigned-issuer` and `issuerRef.kind: ClusterIssuer` with `dnsNames: [myapp.local]`. Add a `:::caution` callout: "Use `kind: ClusterIssuer`, not `kind: Issuer`. The kinder-provisioned issuer is a ClusterIssuer. Using `Issuer` will cause the certificate to stay `READY: False`." Then `kubectl get certificate myapp-tls` with expected output showing `READY: True`.

8. **## Step 6: Create the TLS gateway** -- Gateway YAML with `gatewayClassName: eg`, HTTPS listener on port 443, TLS mode Terminate referencing the `myapp-tls` secret. Then `kubectl get gateway myapp-gateway` with expected output showing `PROGRAMMED: True` and an external IP.

9. **## Step 7: Create an HTTPRoute** -- HTTPRoute YAML with parentRef to `myapp-gateway`, backendRef to `myapp` service port 80.

10. **## Step 8: Verify HTTPS access** -- Get the gateway IP with `kubectl get gateway` jsonpath, then `curl -k https://$GATEWAY_IP`. Expected output: nginx welcome page HTML. Add the macOS/Windows `:::note` with port-forward alternative: show `kubectl get svc -n envoy-gateway-system` to discover the envoy service name, then `kubectl port-forward svc/envoy-default-myapp-gateway-<hash> 8443:443 -n envoy-gateway-system`, then `curl -k https://localhost:8443`.

11. **## Clean up** -- `kinder delete cluster` command.

Content conventions to follow strictly:
- Shell commands in ` ```sh ` blocks
- Kubernetes manifests in ` ```yaml ` blocks
- Expected output in bare ` ``` ` blocks
- Callouts use `:::tip`, `:::note`, `:::caution` with optional `[Title]`
- Use `kubectl apply -f - <<EOF ... EOF` pattern for inline manifest application (consistent with addon pages)
- Every `kubectl get` / `curl` command must have an "Expected output:" label followed by a bare fenced block

Do NOT modify astro.config.mjs or create any new files. Only replace the content of tls-web-app.md.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder/kinder-site && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>tls-web-app.md contains a complete tutorial with: Prerequisites section, 8 numbered steps covering cluster creation through HTTPS verification, macOS/Windows port-forward note, clean up section, all manifests using ClusterIssuer (not Issuer), expected output after every key command, and the site builds without errors</done>
</task>

</tasks>

<verification>
1. `cd /Users/patrykattc/work/git/kinder/kinder-site && npm run build` completes with zero errors
2. `grep -c "ClusterIssuer" kinder-site/src/content/docs/guides/tls-web-app.md` returns at least 2 (YAML + caution callout)
3. `grep -c "localhost:5001" kinder-site/src/content/docs/guides/tls-web-app.md` returns at least 2 (push + deploy)
4. `grep -c "curl -k" kinder-site/src/content/docs/guides/tls-web-app.md` returns at least 1
5. `grep "Coming soon" kinder-site/src/content/docs/guides/tls-web-app.md` returns no results (placeholder removed)
</verification>

<success_criteria>
- TLS tutorial replaces placeholder with 150+ lines of content
- Tutorial covers the complete flow: cluster creation, image push, app deployment, TLS certificate, gateway, HTTPRoute, HTTPS verification
- All YAML manifests use correct resource names (selfsigned-issuer, ClusterIssuer, gatewayClassName: eg)
- macOS/Windows users have a working port-forward alternative
- Site builds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-tutorials/33-01-SUMMARY.md`
</output>
