---
phase: 38-check-infrastructure-and-interface
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - pkg/internal/doctor/runtime.go
  - pkg/internal/doctor/runtime_test.go
  - pkg/internal/doctor/tools.go
  - pkg/internal/doctor/tools_test.go
  - pkg/internal/doctor/gpu.go
  - pkg/internal/doctor/gpu_test.go
  - pkg/internal/doctor/check.go
  - pkg/cmd/kind/doctor/doctor.go
autonomous: true
requirements:
  - INFRA-06
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "kinder doctor produces category-grouped output with Runtime, Tools, GPU sections instead of flat list"
    - "kinder doctor on macOS skips GPU checks with skip status and (linux only) tag instead of silently omitting them"
    - "kinder doctor --output json includes category field on every check and treats skip as ok for exit codes (exit 0)"
    - "All three existing checks produce equivalent diagnostic information as before migration"
    - "Container runtime check detects docker/podman/nerdctl with deps struct injection for testability"
  artifacts:
    - path: "pkg/internal/doctor/runtime.go"
      provides: "Container runtime check migrated to Check interface with deps struct"
      contains: "containerRuntimeCheck"
    - path: "pkg/internal/doctor/tools.go"
      provides: "kubectl check migrated to Check interface with deps struct"
      contains: "kubectlCheck"
    - path: "pkg/internal/doctor/gpu.go"
      provides: "NVIDIA driver, container-toolkit, docker-runtime checks migrated to Check interface"
      contains: "nvidiaDriverCheck"
    - path: "pkg/cmd/kind/doctor/doctor.go"
      provides: "Refactored CLI layer delegating to pkg/internal/doctor/"
      contains: "doctor.RunAllChecks"
    - path: "pkg/internal/doctor/check.go"
      provides: "AllChecks() populated with 3 migrated checks (5 total check results)"
      contains: "newContainerRuntimeCheck"
  key_links:
    - from: "pkg/cmd/kind/doctor/doctor.go"
      to: "pkg/internal/doctor"
      via: "import and call RunAllChecks(), FormatHumanReadable(), FormatJSON(), ExitCodeFromResults()"
      pattern: "doctor\\.RunAllChecks|doctor\\.FormatHumanReadable|doctor\\.FormatJSON"
    - from: "pkg/internal/doctor/check.go"
      to: "pkg/internal/doctor/runtime.go"
      via: "AllChecks() includes newContainerRuntimeCheck()"
      pattern: "newContainerRuntimeCheck"
    - from: "pkg/internal/doctor/check.go"
      to: "pkg/internal/doctor/tools.go"
      via: "AllChecks() includes newKubectlCheck()"
      pattern: "newKubectlCheck"
    - from: "pkg/internal/doctor/check.go"
      to: "pkg/internal/doctor/gpu.go"
      via: "AllChecks() includes newNvidiaDriverCheck(), newNvidiaContainerToolkitCheck(), newNvidiaDockerRuntimeCheck()"
      pattern: "newNvidia"
---

<objective>
Migrate all three existing doctor checks (container runtime, kubectl, NVIDIA GPU) to the Check interface, populate the AllChecks() registry, and refactor doctor.go to delegate to the new pkg/internal/doctor/ package.

Purpose: After this plan, `kinder doctor` produces category-grouped output with Unicode icons through the new infrastructure, all existing checks work identically, and the codebase is ready for 10+ new checks in Phases 39-40 without touching doctor.go again.

Output: Three check implementation files (runtime.go, tools.go, gpu.go) with tests, updated AllChecks() registry, and refactored doctor.go CLI layer.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-check-infrastructure-and-interface/38-CONTEXT.md
@.planning/phases/38-check-infrastructure-and-interface/38-RESEARCH.md
@.planning/phases/38-check-infrastructure-and-interface/38-01-SUMMARY.md

@pkg/cmd/kind/doctor/doctor.go
@pkg/exec/types.go
@pkg/exec/default.go
@pkg/exec/helpers.go
@pkg/cluster/internal/create/actions/testutil/fake.go

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From pkg/internal/doctor/check.go:
```go
type Check interface {
    Name() string
    Category() string
    Platforms() []string
    Run() []Result
}

type Result struct {
    Name     string `json:"name"`
    Category string `json:"category"`
    Status   string `json:"status"`
    Message  string `json:"message,omitempty"`
    Reason   string `json:"reason,omitempty"`
    Fix      string `json:"fix,omitempty"`
}

func AllChecks() []Check
func RunAllChecks() []Result
func ExitCodeFromResults(results []Result) int
```

From pkg/internal/doctor/format.go:
```go
func FormatHumanReadable(w io.Writer, results []Result)
func FormatJSON(results []Result) map[string]interface{}
```

From pkg/exec/types.go:
```go
type Cmd interface {
    Run() error
    SetEnv(...string) Cmd
    SetStdin(io.Reader) Cmd
    SetStdout(io.Writer) Cmd
    SetStderr(io.Writer) Cmd
}
```

From pkg/exec/default.go + helpers.go:
```go
func Command(command string, args ...string) Cmd
func OutputLines(cmd Cmd) ([]string, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate three existing checks to Check interface with deps struct injection</name>
  <files>pkg/internal/doctor/runtime.go, pkg/internal/doctor/runtime_test.go, pkg/internal/doctor/tools.go, pkg/internal/doctor/tools_test.go, pkg/internal/doctor/gpu.go, pkg/internal/doctor/gpu_test.go, pkg/internal/doctor/check.go</files>
  <behavior>
    - Test: containerRuntimeCheck with docker found+working returns ok result with "Container runtime detected (docker)" message
    - Test: containerRuntimeCheck with docker found but not responding returns warn with reason and fix
    - Test: containerRuntimeCheck with no runtimes found returns fail with install URL
    - Test: containerRuntimeCheck has Name()="container-runtime", Category()="Runtime", Platforms()=nil
    - Test: kubectlCheck with kubectl found+working returns ok result
    - Test: kubectlCheck with kubectl not found returns fail with install URL
    - Test: kubectlCheck with kubectl found but version fails returns warn
    - Test: kubectlCheck has Name()="kubectl", Category()="Tools", Platforms()=nil
    - Test: nvidiaDriverCheck with nvidia-smi found returns ok with driver version
    - Test: nvidiaDriverCheck with nvidia-smi not found returns warn
    - Test: nvidiaDriverCheck has Platforms()=[]string{"linux"}
    - Test: nvidiaContainerToolkitCheck with nvidia-ctk found returns ok
    - Test: nvidiaContainerToolkitCheck with nvidia-ctk not found returns warn with install command
    - Test: nvidiaDockerRuntimeCheck with nvidia runtime configured returns ok
    - Test: nvidiaDockerRuntimeCheck with nvidia runtime not configured returns warn with configure command
    - Test: AllChecks() returns 5 checks in order: container-runtime, kubectl, nvidia-driver, nvidia-container-toolkit, nvidia-docker-runtime
  </behavior>
  <action>
Create `runtime.go`:
1. Apache 2.0 copyright header
2. `containerRuntimeCheck` struct with deps:
   - `lookPath func(string) (string, error)` -- defaults to osexec.LookPath
   - `execCmd func(name string, args ...string) exec.Cmd` -- defaults to exec.Command
3. `newContainerRuntimeCheck() Check` constructor wiring real deps
4. Name()="container-runtime", Category()="Runtime", Platforms()=nil (all platforms)
5. Run() logic migrated from existing doctor.go lines 80-108:
   - Try docker, podman, nerdctl in order
   - If found+working: ok result with "Container runtime detected (name)"
   - If found but not responding: warn with reason "The container daemon must be running for cluster creation" and fix "Start the {name} daemon or check its status"
   - If none found: fail with reason "A container runtime (Docker, Podman, or nerdctl) is required" and fix "Install Docker: https://docs.docker.com/get-docker/"
   - Use deps.lookPath and deps.execCmd (NOT direct osexec.LookPath/exec.Command) for testability
   - Use exec.OutputLines() helper with the Cmd from deps.execCmd

Create `tools.go`:
1. Apache 2.0 copyright header
2. `kubectlCheck` struct with deps: lookPath, execCmd (same pattern as runtime)
3. `newKubectlCheck() Check` constructor
4. Name()="kubectl", Category()="Tools", Platforms()=nil
5. Run() logic migrated from existing doctor.go checkKubectl():
   - Not found: fail with reason + fix URL
   - Found but version --client fails: warn with reason + fix
   - Found and working: ok with "kubectl available"

Create `gpu.go`:
1. Apache 2.0 copyright header
2. Three check structs, each with deps: lookPath, execCmd
   - `nvidiaDriverCheck`: Name()="nvidia-driver", Category()="GPU", Platforms()=[]string{"linux"}
   - `nvidiaContainerToolkitCheck`: Name()="nvidia-container-toolkit", Category()="GPU", Platforms()=[]string{"linux"}
   - `nvidiaDockerRuntimeCheck`: Name()="nvidia-docker-runtime", Category()="GPU", Platforms()=[]string{"linux"}
3. Three constructor functions: newNvidiaDriverCheck(), newNvidiaContainerToolkitCheck(), newNvidiaDockerRuntimeCheck()
4. Run() logic migrated from existing doctor.go checkNvidiaDriver(), checkNvidiaContainerToolkit(), checkNvidiaDockerRuntime()
   - Preserve same detection logic: nvidia-smi queries, nvidia-ctk lookup, docker info --format
   - Convert message format to use Result fields (message for detected value, reason for WHY, fix for how-to-fix)
   - nvidiaDockerRuntimeCheck: If docker is not the active runtime (lookPath("docker") fails), return skip result with "(requires docker)" message per research recommendation

Update `check.go`:
- Populate the allChecks slice in AllChecks() with all 5 checks in order:
  1. newContainerRuntimeCheck() -- Runtime category
  2. newKubectlCheck() -- Tools category
  3. newNvidiaDriverCheck() -- GPU category
  4. newNvidiaContainerToolkitCheck() -- GPU category
  5. newNvidiaDockerRuntimeCheck() -- GPU category

Create test files (runtime_test.go, tools_test.go, gpu_test.go):
- Use deps struct injection with fake lookPath and fake execCmd functions (closures returning FakeCmd-like behavior -- do NOT import testutil from pkg/cluster/internal/ as that creates a circular-ish dependency; create simple inline fakes)
- Each test: construct check with injected deps, call Run(), verify result count, status, message content
- Use t.Parallel() on all tests
- Table-driven tests where multiple scenarios exist
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go test ./pkg/internal/doctor/... -count=1 -v</automated>
  </verify>
  <done>All 5 checks migrated to Check interface. Each check uses deps struct for testability. AllChecks() returns 5 checks in Runtime/Tools/GPU category order. All tests pass including Plan 01 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor doctor.go CLI to delegate to pkg/internal/doctor/</name>
  <files>pkg/cmd/kind/doctor/doctor.go</files>
  <action>
Rewrite `pkg/cmd/kind/doctor/doctor.go` to delegate all check logic to the new infrastructure:

1. Keep the Apache 2.0 copyright header
2. Keep the package declaration and cobra command structure
3. Keep the flagpole struct with Output field
4. Keep NewCommand() with same signature: `NewCommand(logger log.Logger, streams cmd.IOStreams) *cobra.Command`
5. Remove ALL inline check functions: checkBinary(), checkKubectl(), checkNvidiaDriver(), checkNvidiaContainerToolkit(), checkNvidiaDockerRuntime()
6. Remove the old result and checkResult types
7. Add import for `"sigs.k8s.io/kind/pkg/internal/doctor"`
8. Rewrite runE() to:
   ```
   a. Validate output flag (same as before)
   b. Call doctor.RunAllChecks() to get []doctor.Result
   c. If output == "json":
      - Encode doctor.FormatJSON(results) via json.NewEncoder(streams.Out)
   d. Else:
      - Call doctor.FormatHumanReadable(streams.ErrOut, results)
   e. os.Exit(doctor.ExitCodeFromResults(results))
   ```
9. The resulting doctor.go should be ~40-50 lines (down from 298), containing ONLY:
   - flagpole struct
   - NewCommand() function
   - runE() function that delegates to doctor package

Do NOT change the command's Use, Short, or Long strings (keep existing). Update the Long description to mention category-grouped output.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && go vet ./... && go test ./pkg/internal/doctor/... -count=1 -v</automated>
  </verify>
  <done>doctor.go reduced from 298 lines to ~40-50 lines. All check logic removed from CLI layer. CLI delegates to doctor.RunAllChecks(), doctor.FormatHumanReadable(), doctor.FormatJSON(), doctor.ExitCodeFromResults(). Full project compiles and all tests pass.</done>
</task>

</tasks>

<verification>
1. `go build ./...` -- full project compiles with no errors
2. `go vet ./...` -- no vet issues
3. `go test ./pkg/internal/doctor/... -count=1 -v` -- all doctor package tests pass
4. `go test ./... -count=1 -short -tags=nointegration` -- full unit test suite passes (no regressions)
5. Visual: `go run . doctor` produces category-grouped output with Unicode icons (on a machine with docker/podman available)
</verification>

<success_criteria>
- 5 checks registered in AllChecks(): container-runtime, kubectl, nvidia-driver, nvidia-container-toolkit, nvidia-docker-runtime
- Categories: Runtime (1 check), Tools (1 check), GPU (3 checks)
- doctor.go is ~40-50 lines, delegating entirely to pkg/internal/doctor/
- Running `kinder doctor` on macOS shows Runtime + Tools checks as ok/warn/fail and GPU checks as skip with "(linux only)" tag
- Running `kinder doctor --output json` produces envelope with "checks" array (each with category field) and "summary" object, exit 0 when all applicable checks pass
- All checks use deps struct injection and have comprehensive unit tests
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/38-check-infrastructure-and-interface/38-02-SUMMARY.md`
</output>
