---
phase: 08-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hack/verify-integration.sh
autonomous: true
requirements:
  - FOUND-04
gap_closure: true

must_haves:
  truths:
    - "hack/verify-integration.sh explicitly sets kubectl context to kind-kinder-integration-test after cluster creation"
    - "kubectl context switch only runs inside the success branch (not after failed cluster creation)"
  artifacts:
    - path: "hack/verify-integration.sh"
      provides: "Integration test script with explicit kubectl context targeting"
      contains: "kubectl config use-context"
  key_links:
    - from: "hack/verify-integration.sh"
      to: "kubectl config"
      via: "use-context call after kinder create cluster succeeds"
      pattern: "kubectl config use-context.*kind-.*CLUSTER_NAME"
---

<objective>
Harden the integration test script by explicitly setting kubectl context to the test cluster after creation, ensuring all subsequent kubectl commands target the correct cluster regardless of the developer's pre-existing context.

Purpose: The script creates a cluster named kinder-integration-test but never sets the kubectl context. If a developer has another cluster active, all kubectl checks (pod readiness, metrics, DNS, dashboard) run against the wrong cluster, producing false results.

Output: Three added lines in hack/verify-integration.sh.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-gap-closure/08-RESEARCH.md

@hack/verify-integration.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add kubectl context targeting to integration script</name>
  <files>hack/verify-integration.sh</files>
  <action>
    In `hack/verify-integration.sh`, add kubectl context targeting inside the success branch of the `kinder create cluster` if-block (after line 94 `pass "SC-1a: kinder create cluster exited 0"`).

    Add these two lines immediately after the `pass` line and before the `else` branch:

    ```bash
    echo "  Setting kubectl context to kind-${CLUSTER_NAME}..."
    kubectl config use-context "kind-${CLUSTER_NAME}"
    ```

    The result should look like:

    ```bash
    if kinder create cluster --name "$CLUSTER_NAME" 2>&1 | tee "$CREATE_LOG"; then
      pass "SC-1a: kinder create cluster exited 0"
      echo "  Setting kubectl context to kind-${CLUSTER_NAME}..."
      kubectl config use-context "kind-${CLUSTER_NAME}"
    else
      fail "SC-1a: kinder create cluster exited non-zero"
    fi
    ```

    IMPORTANT:
    - Place the context switch INSIDE the success branch (after `pass`, before `else`), NOT unconditionally after the if-block. If cluster creation fails, there is no context to switch to.
    - Use `"kind-${CLUSTER_NAME}"` (with the `kind-` prefix) because kind names kubectl contexts as `kind-<cluster-name>`. The cluster name is `kinder-integration-test`, so the context is `kind-kinder-integration-test`.
    - Do NOT use `--context` flags on individual kubectl calls -- the single `use-context` call is sufficient and idiomatic per the research.
    - Do NOT modify any other part of the script.

    After editing, run `bash -n hack/verify-integration.sh` to verify syntax.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && bash -n hack/verify-integration.sh && grep -q 'kubectl config use-context "kind-${CLUSTER_NAME}"' hack/verify-integration.sh && echo "PASS: context targeting present and syntax valid"</automated>
  </verify>
  <done>
    - hack/verify-integration.sh contains `kubectl config use-context "kind-${CLUSTER_NAME}"` inside the success branch of cluster creation
    - Script passes bash syntax check (`bash -n`)
    - Context switch uses the correct `kind-` prefix naming convention
  </done>
</task>

</tasks>

<verification>
1. `bash -n hack/verify-integration.sh` -- syntax check passes
2. `grep -n "use-context" hack/verify-integration.sh` -- shows the new line with correct context name
3. Visual inspection: the `kubectl config use-context` line is inside the `if ... then` block, not after `fi`
</verification>

<success_criteria>
- hack/verify-integration.sh explicitly sets kubectl context to kind-kinder-integration-test after successful cluster creation
- Context switch is conditional on cluster creation success (inside the `then` branch)
- Script passes bash syntax validation
</success_criteria>

<output>
After completion, create `.planning/phases/08-gap-closure/08-02-SUMMARY.md`
</output>
