---
phase: 35-goreleaser-foundation
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - .github/workflows/release.yml
  - hack/release/build/cross.sh
  - hack/release/create.sh
  - hack/ci/build-all.sh
  - hack/ci/push-latest-cli/push-latest-cli.sh
autonomous: true
requirements:
  - REL-05

must_haves:
  truths:
    - "The release workflow uses goreleaser-action v7 instead of cross.sh and softprops"
    - "cross.sh is deleted from the repository"
    - "All three scripts that referenced cross.sh are updated to not call it"
    - "The release workflow triggers on v* tags and has contents:write permission"
    - "fetch-depth: 0 is preserved for changelog generation"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "GoReleaser-based release pipeline"
      contains: "goreleaser/goreleaser-action@v7"
    - path: "hack/release/create.sh"
      provides: "Manual release script updated for goreleaser"
      contains: "goreleaser"
    - path: "hack/ci/build-all.sh"
      provides: "CI build verification without cross.sh"
      contains: "go build"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".goreleaser.yaml"
      via: "goreleaser-action reads the config at repo root"
      pattern: "goreleaser-action"
    - from: ".github/workflows/release.yml"
      to: "GITHUB_TOKEN"
      via: "secrets.GITHUB_TOKEN for release asset upload"
      pattern: "GITHUB_TOKEN"
---

<objective>
Replace the release workflow with goreleaser-action and retire cross.sh atomically across all references.

Purpose: This completes the GoReleaser migration. After this plan, pushing a v* tag to GitHub will trigger a fully automated release pipeline that builds, archives, checksums, generates a changelog, and creates a GitHub Release — all via GoReleaser instead of the old cross.sh + softprops approach.

Output: Updated `release.yml`, deleted `cross.sh`, updated `create.sh`, `build-all.sh`, `push-latest-cli.sh`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-goreleaser-foundation/35-CONTEXT.md
@.planning/phases/35-goreleaser-foundation/35-RESEARCH.md
@.planning/phases/35-goreleaser-foundation/35-01-SUMMARY.md
@.github/workflows/release.yml
@hack/release/build/cross.sh
@hack/release/create.sh
@hack/ci/build-all.sh
@hack/ci/push-latest-cli/push-latest-cli.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace release.yml with goreleaser-action workflow</name>
  <files>.github/workflows/release.yml</files>
  <action>
Replace the entire contents of `.github/workflows/release.yml` with the goreleaser-action workflow. The research file (35-RESEARCH.md) provides the complete replacement YAML.

The new workflow must:
- Trigger on push tags matching `v*` (same as current)
- Set `permissions: contents: write` (same as current)
- Use `actions/checkout@v4` with `fetch-depth: 0` — add a comment: `# REQUIRED: full history needed for GoReleaser changelog generation. DO NOT change to fetch-depth: 1.`
- Use `actions/setup-go@v5` with `go-version-file: .go-version` (simplifies the current two-step version read)
- Use `goreleaser/goreleaser-action@v7` with:
  - `distribution: goreleaser`
  - `version: "~> v2"`
  - `args: release --clean`
  - `env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}`

The workflow should NOT include:
- The old `Read Go version` step (replaced by `go-version-file`)
- The old `hack/release/build/cross.sh` step
- The old `softprops/action-gh-release@v2` step
- Any reference to `bin/kinder-*` glob pattern

Keep the workflow name as `Release` (unchanged).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && grep -q 'goreleaser-action@v7' .github/workflows/release.yml && grep -q 'fetch-depth: 0' .github/workflows/release.yml && ! grep -q 'cross.sh' .github/workflows/release.yml && ! grep -q 'softprops' .github/workflows/release.yml && echo "PASS"</automated>
  </verify>
  <done>
release.yml uses goreleaser/goreleaser-action@v7. No references to cross.sh or softprops remain. fetch-depth: 0 is preserved. go-version-file: .go-version replaces the manual version read step.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete cross.sh and update all referencing scripts</name>
  <files>hack/release/build/cross.sh, hack/release/create.sh, hack/ci/build-all.sh, hack/ci/push-latest-cli/push-latest-cli.sh</files>
  <action>
This task retires cross.sh and updates every script that references it. Per the locked decision, this must happen atomically — no transition period, no fallback.

**1. Delete `hack/release/build/cross.sh`**
Remove the file entirely. This is the core retirement action.

**2. Update `hack/release/create.sh` (line 71)**
Current: `make clean && ./hack/release/build/cross.sh`
Replace with: `make clean && goreleaser build --clean`

This script is used for manual release preparation. The `goreleaser build --clean` command builds all platform binaries locally using the `.goreleaser.yaml` config. The rest of the script (version bumping, tagging, git push instructions) remains unchanged.

Also update the final echo instructions: the script currently says "Create a GitHub release from the pushed tag" — update to say "Push the tag; the release workflow will create the GitHub Release automatically" since GoReleaser now handles release creation.

**3. Update `hack/ci/build-all.sh` (line 26)**
Current: `hack/release/build/cross.sh`
Replace with: `go build -v ./...`

This script is used for CI verification ("simple CI script to verify kind's own sources"). Its purpose is to confirm the code compiles — it does not need cross-compilation. A simple `go build -v ./...` is sufficient and faster. This matches the research recommendation.

**4. Update `hack/ci/push-latest-cli/push-latest-cli.sh` (line 42)**
Current: `hack/release/build/cross.sh`

This script uploads binaries to a GCS bucket (`k8s-staging-kind`) for Kubernetes CI consumption. This is upstream kind infrastructure that is NOT used by the kinder fork (the bucket name references the upstream project).

Replace line 42 with a comment and early exit:
```bash
# NOTE: cross.sh has been retired in favor of GoReleaser.
# This script uploaded to the k8s-staging-kind GCS bucket for upstream kind CI.
# It is not used by the kinder fork. Kept for reference but disabled.
echo "push-latest-cli.sh: disabled — cross.sh retired, GoReleaser handles releases"
exit 0
```

Place this block after the `cd "${REPO_ROOT}"` line (line 21) so the script exits cleanly without trying to build or upload anything. Keep the rest of the file intact as reference.

**5. Verify no remaining cross.sh references in the codebase**
After all changes, grep the entire repo for `cross.sh` — only matches should be in planning/research docs, not in any executable script or workflow.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && test ! -f hack/release/build/cross.sh && grep -q 'goreleaser build' hack/release/create.sh && grep -q 'go build' hack/ci/build-all.sh && grep -q 'exit 0' hack/ci/push-latest-cli/push-latest-cli.sh && ! grep -rl 'cross\.sh' .github/ hack/ --include='*.sh' --include='*.yml' --include='*.yaml' 2>/dev/null && echo "PASS"</automated>
  </verify>
  <done>
cross.sh is deleted. hack/release/create.sh uses goreleaser build --clean. hack/ci/build-all.sh uses go build -v ./... instead. hack/ci/push-latest-cli.sh exits early with an explanatory message. No executable script or workflow references cross.sh.
  </done>
</task>

</tasks>

<verification>
1. `grep -r 'cross\.sh' .github/ hack/` returns zero matches in executable files
2. `hack/release/build/cross.sh` does not exist
3. `.github/workflows/release.yml` contains `goreleaser-action@v7` and no `softprops`
4. `hack/release/create.sh` references `goreleaser build` instead of cross.sh
5. `hack/ci/build-all.sh` uses `go build` instead of cross.sh
6. `hack/ci/push-latest-cli/push-latest-cli.sh` exits 0 immediately
7. `goreleaser check` still passes (config from Plan 01 unchanged)
</verification>

<success_criteria>
- release.yml uses goreleaser/goreleaser-action@v7 (REL-05)
- cross.sh is deleted and all 3 referencing scripts are updated (REL-05, locked decision: atomic retirement)
- No softprops/action-gh-release reference remains (REL-05)
- fetch-depth: 0 preserved for changelog generation
- goreleaser check still passes after all changes
</success_criteria>

<output>
After completion, create `.planning/phases/35-goreleaser-foundation/35-02-SUMMARY.md`
</output>
