---
phase: 25-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - pkg/build/nodeimage/buildcontext.go
  - pkg/cluster/internal/create/create.go
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-05

must_haves:
  truths:
    - "go.mod declares go 1.23 as minimum version"
    - "golang.org/x/sys is at v0.41.0"
    - "No reference to rand.NewSource exists in the codebase"
    - "go build ./... succeeds with the updated go.mod"
  artifacts:
    - path: "go.mod"
      provides: "Updated Go module definition"
      contains: "go 1.23"
    - path: "go.sum"
      provides: "Updated dependency checksums"
    - path: "pkg/build/nodeimage/buildcontext.go"
      provides: "Dead code removed — rand.Int32() replaces rand.New(rand.NewSource(...))"
    - path: "pkg/cluster/internal/create/create.go"
      provides: "Dead code removed — rand.Intn() replaces rand.New(rand.NewSource(...))"
  key_links:
    - from: "go.mod"
      to: "go.sum"
      via: "go mod tidy consistency"
      pattern: "go 1\\.23"
---

<objective>
Bump the Go module minimum to 1.23, update golang.org/x/sys to v0.41.0, and remove dead rand.NewSource code that was comment-gated for pre-1.23 compatibility.

Purpose: Establish a clean Go dependency baseline for v1.4. The go 1.23 minimum unlocks auto-seeded global rand, making explicit rand.NewSource calls dead code.
Output: Updated go.mod/go.sum, simplified rand usage in two files.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-foundation/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump go.mod to 1.23 and update golang.org/x/sys</name>
  <files>go.mod, go.sum</files>
  <action>
1. Edit go.mod: change `go 1.21.0` to `go 1.23` (drop the patch version — Go module directives use major.minor).
2. Run `go get golang.org/x/sys@v0.41.0` to update x/sys from v0.6.0 to v0.41.0.
3. Run `go mod tidy` to clean up go.sum and verify consistency.
4. Verify no other indirect deps need updating by checking `go build ./...` succeeds.

IMPORTANT: The `toolchain go1.26.0` line in go.mod should remain unchanged — that is the compiler version, not the language minimum.
IMPORTANT: Do NOT remove the NOTE comment block above the `go` directive explaining the difference between language version and compiler version — it is useful documentation.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && grep '^go 1.23' go.mod && grep 'golang.org/x/sys v0.41.0' go.mod && go build ./...</automated>
  </verify>
  <done>go.mod declares `go 1.23`, golang.org/x/sys is v0.41.0, `go build ./...` passes</done>
</task>

<task type="auto">
  <name>Task 2: Remove rand.NewSource dead code</name>
  <files>pkg/build/nodeimage/buildcontext.go, pkg/cluster/internal/create/create.go</files>
  <action>
1. In `pkg/build/nodeimage/buildcontext.go` around line 346:
   - Replace `random := rand.New(rand.NewSource(time.Now().UnixNano())).Int31()` with `random := rand.Int32()`
   - Remove the NOTE comment above it that says "explicit seeding is required while the module minimum is Go 1.17" — this is no longer true after the 1.23 bump.
   - Remove the `time` import if it is no longer used elsewhere in the file (check first — it likely IS used elsewhere, so keep it).
   - The `math/rand` import should remain (now using global functions instead of creating sources). Verify the import is `math/rand/v2` or just `math/rand` — use whichever is already in use. Since this is a Go 1.23+ module, `rand.Int32()` is available from `math/rand/v2`, but the existing code uses `math/rand`. Use `rand.Int32()` from `math/rand` which is available in Go 1.22+ (was added as part of the global rand improvements). Actually, `Int32()` was added to `math/rand/v2` — in `math/rand` it's `Int31()` but that's deprecated messaging. The safest approach: keep `math/rand` import and use `rand.Int32()` which redirects to v2 in Go 1.22+. If the compiler complains, use `rand.Int31()` instead (the function still exists, just the NewSource pattern is dead).

   Correction per research: use `rand.Int32()` — available in `math/rand` since Go 1.22. If that doesn't compile, fall back to `rand.Int31()` which has always existed.

2. In `pkg/cluster/internal/create/create.go` around line 276:
   - Replace `r := rand.New(rand.NewSource(time.Now().UTC().UnixNano()))` and `s := salutations[r.Intn(len(salutations))]` with just `s := salutations[rand.Intn(len(salutations))]`
   - Remove the NOTE comment about explicit seeding.
   - Check if `time` import is still used elsewhere in the file — keep if so, remove if not.

3. Run `go build ./...` to confirm compilation succeeds.
4. Grep the entire codebase for `rand.NewSource` to confirm no remaining occurrences in Go source files (exclude vendor, .planning, etc.).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && ! grep -r 'rand\.NewSource' --include='*.go' pkg/ cmd/ 2>/dev/null</automated>
  </verify>
  <done>No reference to rand.NewSource in any Go source under pkg/ or cmd/. Both files compile cleanly. rand.Int32()/rand.Intn() used with global auto-seeded source.</done>
</task>

</tasks>

<verification>
1. `go build ./...` succeeds
2. `grep '^go 1.23' go.mod` matches
3. `grep 'golang.org/x/sys v0.41.0' go.mod` matches
4. No `rand.NewSource` in Go source files
5. `go vet ./...` passes
</verification>

<success_criteria>
- go.mod minimum is 1.23
- golang.org/x/sys is v0.41.0
- Zero references to rand.NewSource in Go source
- `go build ./...` and `go vet ./...` pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-foundation/25-01-SUMMARY.md`
</output>
