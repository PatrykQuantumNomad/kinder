---
phase: 25-foundation
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - pkg/internal/kindversion/version.go
  - pkg/cmd/kind/version/version.go
  - pkg/cluster/provider.go
  - pkg/cmd/kind/root.go
  - Makefile
  - hack/release/create.sh
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "pkg/cluster/provider.go does NOT import pkg/cmd/kind/version"
    - "pkg/internal/kindversion/version.go contains Version(), DisplayVersion(), and the linker-injected vars"
    - "pkg/cmd/kind/version/version.go retains only NewCommand() and imports from kindversion"
    - "kinder version prints the correct version string after Makefile linker flag update"
  artifacts:
    - path: "pkg/internal/kindversion/version.go"
      provides: "Version constants and functions in internal package"
      contains: "package kindversion"
    - path: "pkg/cmd/kind/version/version.go"
      provides: "Thin command wrapper importing from kindversion"
      contains: "kindversion"
    - path: "pkg/cluster/provider.go"
      provides: "Provider importing version from internal package"
      contains: "kindversion"
    - path: "Makefile"
      provides: "Updated linker -X flags targeting kindversion package"
      contains: "pkg/internal/kindversion"
    - path: "hack/release/create.sh"
      provides: "Updated VERSION_FILE path for release script"
      contains: "pkg/internal/kindversion"
  key_links:
    - from: "pkg/cluster/provider.go"
      to: "pkg/internal/kindversion"
      via: "import for version.Version()"
      pattern: "kindversion\\.Version"
    - from: "Makefile"
      to: "pkg/internal/kindversion"
      via: "KIND_VERSION_PKG linker variable"
      pattern: "KIND_VERSION_PKG.*kindversion"
    - from: "pkg/cmd/kind/version/version.go"
      to: "pkg/internal/kindversion"
      via: "import for Version/DisplayVersion functions"
      pattern: "kindversion\\.Version|kindversion\\.DisplayVersion"
---

<objective>
Fix the layer violation where pkg/cluster/provider.go imports from pkg/cmd/kind/version/ (a CLI-layer package). Move version constants and functions to pkg/internal/kindversion/, leaving only the NewCommand() cobra wrapper in the original location.

Purpose: The cluster management layer should not depend on CLI-layer packages. This creates a clean import direction: cmd -> cluster -> internal, never cluster -> cmd.
Output: New pkg/internal/kindversion/ package, updated imports, updated linker flags.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-foundation/25-RESEARCH.md
</context>

<interfaces>
<!-- Current version package exports that must move to kindversion: -->
From pkg/cmd/kind/version/version.go:
```go
func Version() string                    // Must move to kindversion
func DisplayVersion() string             // Must move to kindversion
const versionCore = "0.3.0"             // Must move to kindversion
var versionPreRelease = "alpha"          // Must move to kindversion
var gitCommitCount = ""                  // Must move to kindversion (linker target)
var gitCommit = ""                       // Must move to kindversion (linker target)
func NewCommand(...) *cobra.Command      // STAYS in pkg/cmd/kind/version
func truncate(s string, maxLen int) string // Must move to kindversion (used by Version)
func version(core, preRelease, ...) string // Must move to kindversion (used by Version)
```

<!-- Existing pkg/internal/version/ (DIFFERENT package — Kubernetes version parsing): -->
From pkg/internal/version/version.go:
```go
package version
type Version struct { ... }
func ParseGeneric(str string) (*Version, error)
func ParseSemantic(str string) (*Version, error)
```
Note: This is a DIFFERENT package for K8s version parsing. The new package must be named `kindversion` to avoid conflict.

<!-- Current import in provider.go: -->
From pkg/cluster/provider.go:
```go
import "sigs.k8s.io/kind/pkg/cmd/kind/version"
// Used as: version.Version()
```

<!-- Current import in root.go: -->
From pkg/cmd/kind/root.go:
```go
import "sigs.k8s.io/kind/pkg/cmd/kind/version"
// Used as: version.Version() and version.NewCommand(...)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create pkg/internal/kindversion and split version package</name>
  <files>pkg/internal/kindversion/version.go, pkg/cmd/kind/version/version.go</files>
  <action>
1. Create directory `pkg/internal/kindversion/`.

2. Create `pkg/internal/kindversion/version.go` with:
   - Package declaration: `package kindversion`
   - Copyright header (same Apache 2.0 as other files)
   - Package doc comment: `// Package kindversion contains the kinder CLI version constants and functions.`
   - Move FROM pkg/cmd/kind/version/version.go:
     - `func Version() string` (exported)
     - `func version(core, preRelease, commit, commitCount string) string` (unexported helper)
     - `func DisplayVersion() string` (exported)
     - `const versionCore = "0.3.0"` (unexported constant)
     - `var versionPreRelease = "alpha"` (unexported var — linker target)
     - `var gitCommitCount = ""` (unexported var — linker target)
     - `var gitCommit = ""` (unexported var — linker target)
     - `func truncate(s string, maxLen int) string` (unexported helper)
   - Required imports: `"fmt"`, `"runtime"` (for DisplayVersion)
   - Do NOT import cobra or any cmd-layer packages

3. Rewrite `pkg/cmd/kind/version/version.go` to be a thin wrapper:
   - Keep: `package version`, copyright header, package doc comment
   - Keep: `func NewCommand(logger log.Logger, streams cmd.IOStreams) *cobra.Command` — this is the CLI command
   - Change NewCommand's RunE to call `kindversion.Version()` and `kindversion.DisplayVersion()` instead of the local functions
   - Add import: `"sigs.k8s.io/kind/pkg/internal/kindversion"`
   - Remove: ALL moved functions and variables (Version, DisplayVersion, version, versionCore, versionPreRelease, gitCommit, gitCommitCount, truncate)
   - The file should only have: package declaration, imports, NewCommand function

4. Verify `go build ./...` compiles.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && test -f pkg/internal/kindversion/version.go && go build ./...</automated>
  </verify>
  <done>pkg/internal/kindversion/version.go exists with Version(), DisplayVersion(), and all version vars. pkg/cmd/kind/version/version.go only has NewCommand(). go build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update imports, linker flags, and release script</name>
  <files>pkg/cluster/provider.go, pkg/cmd/kind/root.go, Makefile, hack/release/create.sh</files>
  <action>
1. In `pkg/cluster/provider.go`:
   - Change import from `"sigs.k8s.io/kind/pkg/cmd/kind/version"` to `"sigs.k8s.io/kind/pkg/internal/kindversion"`
   - Update all usages: `version.Version()` -> `kindversion.Version()`
   - The only usage is in the `Create` method where it logs the version. Grep for `version.` in the file to find all call sites.

2. In `pkg/cmd/kind/root.go`:
   - The import `"sigs.k8s.io/kind/pkg/cmd/kind/version"` must STAY because root.go uses `version.NewCommand(logger, streams)` and `version.Version()`.
   - HOWEVER, `version.Version()` is used on line 56 (`Version: version.Version()`). This now needs to come from kindversion since Version() moved there.
   - Add import: `"sigs.k8s.io/kind/pkg/internal/kindversion"`
   - Change line 56: `Version: version.Version()` -> `Version: kindversion.Version()`
   - Keep the `version` import for `version.NewCommand(logger, streams)` on line 81.

3. In `Makefile` line 60:
   - Change: `KIND_VERSION_PKG:=sigs.k8s.io/kind/pkg/cmd/kind/version`
   - To: `KIND_VERSION_PKG:=sigs.k8s.io/kind/pkg/internal/kindversion`
   - This updates the linker -X flags to inject gitCommit and gitCommitCount into the new package location.

4. In `hack/release/create.sh` line 44:
   - Change: `VERSION_FILE="./pkg/cmd/kind/version/version.go"`
   - To: `VERSION_FILE="./pkg/internal/kindversion/version.go"`
   - This is the file the release script uses sed on to update versionCore and versionPreRelease.

5. Verify the full build works: `go build ./...`
6. Verify linker flags work: `make build && ./bin/kinder version` should print a version string.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && ! grep -q 'pkg/cmd/kind/version' pkg/cluster/provider.go && grep -q 'kindversion' pkg/cluster/provider.go && grep -q 'pkg/internal/kindversion' Makefile && grep -q 'pkg/internal/kindversion' hack/release/create.sh</automated>
  </verify>
  <done>provider.go imports kindversion (not cmd/version). root.go uses kindversion.Version() for cobra Version field. Makefile KIND_VERSION_PKG points to pkg/internal/kindversion. Release script VERSION_FILE points to new location. `go build ./...` passes. `make build && ./bin/kinder version` prints version string.</done>
</task>

</tasks>

<verification>
1. `go build ./...` succeeds
2. `grep -r 'pkg/cmd/kind/version' pkg/cluster/` returns nothing (layer violation fixed)
3. `pkg/internal/kindversion/version.go` exists with Version() and DisplayVersion()
4. `pkg/cmd/kind/version/version.go` has only NewCommand()
5. `make build && ./bin/kinder version` prints version string
6. Makefile KIND_VERSION_PKG references `pkg/internal/kindversion`
7. hack/release/create.sh VERSION_FILE references `pkg/internal/kindversion`
</verification>

<success_criteria>
- No import of pkg/cmd/kind/version from pkg/cluster/
- Version functions live in pkg/internal/kindversion/
- Linker -X flags target the new package path
- `kinder version` prints correct version string
- Release script updated for new file location
</success_criteria>

<output>
After completion, create `.planning/phases/25-foundation/25-03-SUMMARY.md`
</output>
