---
phase: 25-foundation
plan: 04
type: execute
wave: 3
depends_on: ["25-03"]
files_modified:
  - pkg/cluster/internal/providers/docker/network.go
  - pkg/cluster/internal/providers/podman/network.go
  - pkg/cluster/internal/providers/nerdctl/network.go
  - pkg/cluster/provider.go
  - pkg/cluster/internal/create/actions/installdashboard/dashboard.go
  - pkg/cluster/internal/create/actions/installmetallb/subnet_test.go
autonomous: true
requirements:
  - FOUND-06
  - FOUND-07
  - FOUND-08
  - FOUND-09
  - FOUND-10

must_haves:
  truths:
    - "SHA-256 is used for subnet generation in all three providers"
    - "Log directory permissions are 0755 (not os.ModePerm/0777)"
    - "Dashboard token line logs at V(1), other dashboard lines remain at V(0)"
    - "Error variable is named ErrNoNodeProviderDetected (Go convention)"
    - "No custom contains/containsStr helper in subnet_test.go"
  artifacts:
    - path: "pkg/cluster/internal/providers/docker/network.go"
      provides: "SHA-256 subnet generation"
      contains: "crypto/sha256"
    - path: "pkg/cluster/internal/providers/podman/network.go"
      provides: "SHA-256 subnet generation"
      contains: "crypto/sha256"
    - path: "pkg/cluster/internal/providers/nerdctl/network.go"
      provides: "SHA-256 subnet generation"
      contains: "crypto/sha256"
    - path: "pkg/cluster/provider.go"
      provides: "Renamed error var + fixed dir permissions"
      contains: "ErrNoNodeProviderDetected"
    - path: "pkg/cluster/internal/create/actions/installdashboard/dashboard.go"
      provides: "Token at V(1)"
      contains: "V(1)"
    - path: "pkg/cluster/internal/create/actions/installmetallb/subnet_test.go"
      provides: "Uses strings.Contains instead of custom helper"
      contains: "strings.Contains"
  key_links:
    - from: "pkg/cluster/provider.go"
      to: "ErrNoNodeProviderDetected"
      via: "error variable declaration and usage"
      pattern: "ErrNoNodeProviderDetected"
    - from: "pkg/cluster/internal/providers/docker/network.go"
      to: "crypto/sha256"
      via: "import and usage in generateULASubnetFromName"
      pattern: "sha256\\.New"
---

<objective>
Apply five targeted code quality fixes: SHA-1 to SHA-256 for subnet generation, os.ModePerm to 0755 for log directories, dashboard token log level V(0) to V(1), error variable rename to Go convention, and removal of redundant test helper.

Purpose: Address code quality findings from the codebase review. All changes are mechanical and localized.
Output: Cleaner, more correct code across six files.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-foundation/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SHA-256 subnet generation + log dir permissions + dashboard token log level</name>
  <files>pkg/cluster/internal/providers/docker/network.go, pkg/cluster/internal/providers/podman/network.go, pkg/cluster/internal/providers/nerdctl/network.go, pkg/cluster/provider.go, pkg/cluster/internal/create/actions/installdashboard/dashboard.go</files>
  <action>
**FOUND-06: SHA-1 to SHA-256 (3 files)**

In each of the three provider network.go files, make two changes:

1. Change import: `"crypto/sha1"` -> `"crypto/sha256"`
2. Change hash constructor: `sha1.New()` -> `sha256.New()`

Files and locations:
- `pkg/cluster/internal/providers/docker/network.go`: import line ~21, usage line ~340
- `pkg/cluster/internal/providers/podman/network.go`: import line ~20, usage line ~133
- `pkg/cluster/internal/providers/nerdctl/network.go`: import line ~20, usage line ~180

The function `generateULASubnetFromName` only uses bytes 2-7 of the hash output for the IPv6 ULA address, so SHA-256 vs SHA-1 only affects which bytes are produced — the algorithm structure is identical. Subnet values WILL change (different hash = different subnets), which is acceptable since clusters are transient.

Do NOT change any other code in these functions — only the hash algorithm.

**FOUND-07: Log directory permissions (1 file)**

In `pkg/cluster/provider.go` around line 257:
- Change: `os.MkdirAll(dir, os.ModePerm)` to `os.MkdirAll(dir, 0755)`
- This changes log directory permissions from 0777 to 0755 (owner rwx, group/other rx)
- Do NOT change any other os.ModePerm usage in the codebase — research confirmed only this site is for log directories. The usages in common/logs.go and fs/fs.go are for files/kubeconfig and are out of scope.
- If `os` is already imported, no import change needed. If `os.ModePerm` was the only usage of `os` constants, verify the import is still needed (it is — `os.MkdirAll` uses the `os` package).

**FOUND-08: Dashboard token log level (1 file)**

In `pkg/cluster/internal/create/actions/installdashboard/dashboard.go` around line 108:
- Change ONLY the token line: `ctx.Logger.V(0).Infof("  Token: %s", token)` -> `ctx.Logger.V(1).Infof("  Token: %s", token)`
- Do NOT change the other V(0) lines (106, 107, 109, 110, 111) — those are the blank line, "Dashboard:" header, port-forward instructions, URL, and trailing blank line. Those should remain at V(0) for user-facing output.
- The token is sensitive and should only show at verbose level.

After all changes, run `go build ./...` and `go vet ./...`.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && go vet ./... && grep -c 'sha256' pkg/cluster/internal/providers/docker/network.go pkg/cluster/internal/providers/podman/network.go pkg/cluster/internal/providers/nerdctl/network.go | grep -v ':0$' | wc -l | tr -d ' ' | grep '^3$' && grep '0755' pkg/cluster/provider.go | grep -q MkdirAll && grep 'V(1)' pkg/cluster/internal/create/actions/installdashboard/dashboard.go | grep -q Token</automated>
  </verify>
  <done>All three provider network.go files use crypto/sha256 instead of crypto/sha1. Log directory uses 0755 permissions. Dashboard token logs at V(1). go build and go vet pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rename error var to Go convention + remove redundant test helper</name>
  <files>pkg/cluster/provider.go, pkg/cluster/internal/create/actions/installmetallb/subnet_test.go</files>
  <action>
**FOUND-09: Error var rename (1 file, 2 sites)**

In `pkg/cluster/provider.go`:
1. Line ~102: Rename the variable declaration:
   `var NoNodeProviderDetectedError = errors.NewWithoutStack(...)`
   -> `var ErrNoNodeProviderDetected = errors.NewWithoutStack(...)`

2. Line ~129: Update the usage:
   `return nil, errors.WithStack(NoNodeProviderDetectedError)`
   -> `return nil, errors.WithStack(ErrNoNodeProviderDetected)`

3. Update the comment above the variable (line ~100-101) to reference the new name:
   `// ErrNoNodeProviderDetected indicates that we could not autolocate an available`
   `// NodeProvider backend on the host`

4. Search the entire codebase for other references to `NoNodeProviderDetectedError` — it may be referenced in other packages. If so, update those too. Likely places: tests, cmd layer code that checks for this error.
   Run: `grep -r 'NoNodeProviderDetectedError' --include='*.go' .`

IMPORTANT: This is an exported variable, so renaming it is technically a breaking change for external callers. Since kinder is a fork and not consumed as a library, this is acceptable.

**FOUND-10: Remove redundant contains helper (1 file)**

In `pkg/cluster/internal/create/actions/installmetallb/subnet_test.go`:
1. Add import `"strings"` if not already present.
2. Replace all calls to `contains(err.Error(), tc.errContains)` with `strings.Contains(err.Error(), tc.errContains)`.
   There are two call sites (around lines 97 and 186).
3. Delete the `contains` function (around line 203-206) and the `containsStr` function (around lines 208-214).
4. Run the tests: `go test ./pkg/cluster/internal/create/actions/installmetallb/...`

After all changes, run `go build ./...` to verify.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && grep -q 'ErrNoNodeProviderDetected' pkg/cluster/provider.go && ! grep -q 'NoNodeProviderDetectedError' pkg/cluster/provider.go && grep -q 'strings.Contains' pkg/cluster/internal/create/actions/installmetallb/subnet_test.go && ! grep -q 'func contains' pkg/cluster/internal/create/actions/installmetallb/subnet_test.go && go test ./pkg/cluster/internal/create/actions/installmetallb/...</automated>
  </verify>
  <done>Error var renamed to ErrNoNodeProviderDetected (Go convention). No reference to NoNodeProviderDetectedError remains. subnet_test.go uses strings.Contains, custom contains/containsStr helpers deleted. All tests pass.</done>
</task>

</tasks>

<verification>
1. `go build ./...` and `go vet ./...` pass
2. `grep -r 'sha1' --include='*.go' pkg/cluster/internal/providers/` returns nothing
3. `grep 'os.ModePerm' pkg/cluster/provider.go` returns nothing
4. `grep 'V(1).*Token' pkg/cluster/internal/create/actions/installdashboard/dashboard.go` matches
5. `grep 'NoNodeProviderDetectedError' pkg/cluster/provider.go` returns nothing
6. `grep 'func contains' pkg/cluster/internal/create/actions/installmetallb/subnet_test.go` returns nothing
7. `go test ./pkg/cluster/internal/create/actions/installmetallb/...` passes
</verification>

<success_criteria>
- SHA-256 in all three provider network files
- 0755 for log directory permissions
- Dashboard token at V(1), other dashboard lines at V(0)
- ErrNoNodeProviderDetected follows Go naming convention
- No custom contains helper in subnet_test.go
- All tests pass, build clean
</success_criteria>

<output>
After completion, create `.planning/phases/25-foundation/25-04-SUMMARY.md`
</output>
