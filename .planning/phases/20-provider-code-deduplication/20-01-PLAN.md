---
phase: 20-provider-code-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/cluster/internal/providers/common/node.go
  - pkg/cluster/internal/providers/common/constants.go
  - pkg/cluster/internal/providers/docker/provider.go
  - pkg/cluster/internal/providers/nerdctl/provider.go
  - pkg/cluster/internal/providers/podman/provider.go
  - pkg/cluster/internal/providers/docker/node.go
  - pkg/cluster/internal/providers/nerdctl/node.go
  - pkg/cluster/internal/providers/podman/node.go
  - go.mod
autonomous: true
requirements:
  - PROV-01
  - PROV-03

must_haves:
  truths:
    - "All three providers (docker, nerdctl, podman) construct nodes via common.Node with BinaryName parameter"
    - "docker/node.go, nerdctl/node.go, and podman/node.go no longer exist"
    - "NodeRoleLabelKey is exported from common/constants.go and used by common/node.go Role()"
    - "go.mod declares go 1.21.0 with toolchain go1.26.0"
    - "go build ./... and go test ./... both pass with no regressions"
  artifacts:
    - path: "pkg/cluster/internal/providers/common/node.go"
      provides: "Shared Node struct and unexported nodeCmd struct"
      exports: ["Node"]
      contains: "BinaryName string"
    - path: "pkg/cluster/internal/providers/common/constants.go"
      provides: "Shared constants including NodeRoleLabelKey"
      contains: "NodeRoleLabelKey"
    - path: "go.mod"
      provides: "Updated Go minimum version"
      contains: "go 1.21.0"
  key_links:
    - from: "pkg/cluster/internal/providers/docker/provider.go"
      to: "pkg/cluster/internal/providers/common/node.go"
      via: "node() factory returns &common.Node{BinaryName: \"docker\"}"
      pattern: "common\\.Node\\{"
    - from: "pkg/cluster/internal/providers/nerdctl/provider.go"
      to: "pkg/cluster/internal/providers/common/node.go"
      via: "node() factory returns &common.Node{BinaryName: p.binaryName}"
      pattern: "common\\.Node\\{"
    - from: "pkg/cluster/internal/providers/podman/provider.go"
      to: "pkg/cluster/internal/providers/common/node.go"
      via: "node() factory returns &common.Node{BinaryName: \"podman\"}"
      pattern: "common\\.Node\\{"
    - from: "pkg/cluster/internal/providers/common/node.go"
      to: "pkg/cluster/internal/providers/common/constants.go"
      via: "Node.Role() uses NodeRoleLabelKey"
      pattern: "NodeRoleLabelKey"
---

<objective>
Extract the shared Node and nodeCmd structs from docker/node.go, nerdctl/node.go, and podman/node.go into a single common/node.go with a BinaryName field, then update all three provider factory methods to use common.Node. Also update go.mod to go 1.21.0 with toolchain go1.26.0.

Purpose: Eliminate ~500 lines of copy-paste duplication across three provider node.go files and modernize go.mod to support the toolchain directive.
Output: common/node.go, updated common/constants.go, updated provider.go files, deleted per-provider node.go files, updated go.mod.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-provider-code-deduplication/20-RESEARCH.md

@pkg/cluster/internal/providers/common/constants.go
@pkg/cluster/internal/providers/nerdctl/node.go
@pkg/cluster/internal/providers/docker/node.go
@pkg/cluster/internal/providers/podman/node.go
@pkg/cluster/internal/providers/docker/provider.go
@pkg/cluster/internal/providers/nerdctl/provider.go
@pkg/cluster/internal/providers/podman/provider.go
@pkg/cluster/internal/providers/docker/constants.go
@pkg/cluster/internal/providers/nerdctl/constants.go
@pkg/cluster/internal/providers/podman/constants.go
@go.mod

<interfaces>
<!-- Nerdctl's node.go is the template -- it already has binaryName string field. -->
<!-- Docker and podman hardcode their binary name as literals. -->
<!-- All three node.go files are structurally identical except for the binary name. -->

From pkg/cluster/nodes/types.go (the Node interface these structs implement):
```go
type Node interface {
    String() string
    Role() (string, error)
    IP() (ipv4 string, ipv6 string, err error)
    Command(command string, args ...string) exec.Cmd
    CommandContext(ctx context.Context, command string, args ...string) exec.Cmd
    SerialLogs(w io.Writer) error
}
```

From pkg/exec/ (the Cmd interface nodeCmd implements):
```go
type Cmd interface {
    Run() error
    SetEnv(env ...string) Cmd
    SetStdin(r io.Reader) Cmd
    SetStdout(w io.Writer) Cmd
    SetStderr(w io.Writer) Cmd
}
```

Current constant values (identical in all three providers):
```go
const nodeRoleLabelKey = "io.x-k8s.kind.role"
const clusterLabelKey = "io.x-k8s.kind.cluster"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create common/node.go and update common/constants.go</name>
  <files>
    pkg/cluster/internal/providers/common/node.go
    pkg/cluster/internal/providers/common/constants.go
  </files>
  <action>
1. Add `NodeRoleLabelKey` to `common/constants.go`:
   - Add `NodeRoleLabelKey = "io.x-k8s.kind.role"` as an exported constant alongside the existing `APIServerInternalPort`.
   - Keep the Apache 2.0 license header. Keep the package comment simple.

2. Create `common/node.go` using nerdctl/node.go as the template (it already has `binaryName string`):
   - Package: `common`
   - Export `Node` struct with fields: `Name string`, `BinaryName string`
   - Keep `nodeCmd` struct UNEXPORTED (lowercase) — it is an implementation detail returned as `exec.Cmd` interface.
   - The `nodeCmd` struct needs a `binaryName string` field (like nerdctl's version).
   - `Node.Role()` must reference `NodeRoleLabelKey` (from constants.go in same package), NOT a hardcoded string.
   - `Node.String()` returns `n.Name`
   - `Node.IP()` uses `n.BinaryName` for the exec.Command call
   - `Node.Command()` returns `&nodeCmd{binaryName: n.BinaryName, nameOrID: n.Name, command: command, args: args}`
   - `Node.CommandContext()` same but with ctx field set
   - `Node.SerialLogs()` uses `n.BinaryName` for exec.Command call
   - `nodeCmd.Run()` uses `c.binaryName` for both the context and non-context exec.Command calls (like nerdctl's version, NOT docker's which hardcodes "docker")
   - Include all SetEnv/SetStdin/SetStdout/SetStderr methods on nodeCmd
   - Include Apache 2.0 license header with "Copyright 2019 The Kubernetes Authors."
   - Imports needed: "context", "fmt", "io", "strings", "sigs.k8s.io/kind/pkg/errors", "sigs.k8s.io/kind/pkg/exec"

3. After creating both files, run `go build ./pkg/cluster/internal/providers/common/...` to verify the common package compiles. Fix any issues before proceeding.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./pkg/cluster/internal/providers/common/...</automated>
  </verify>
  <done>common/node.go exists with exported Node struct and unexported nodeCmd, common/constants.go has NodeRoleLabelKey, common package compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Update provider factories, delete old node.go files, update go.mod</name>
  <files>
    pkg/cluster/internal/providers/docker/provider.go
    pkg/cluster/internal/providers/nerdctl/provider.go
    pkg/cluster/internal/providers/podman/provider.go
    pkg/cluster/internal/providers/docker/node.go
    pkg/cluster/internal/providers/nerdctl/node.go
    pkg/cluster/internal/providers/podman/node.go
    go.mod
  </files>
  <action>
1. Update `docker/provider.go`:
   - The `node()` method (line ~230) must change from `return &node{name: name}` to `return &common.Node{Name: name, BinaryName: "docker"}`.
   - Ensure `common` import exists (it already does: `"sigs.k8s.io/kind/pkg/cluster/internal/providers/common"`).
   - Verify no other references to the local `node` type exist in provider.go (there should be none besides the factory).

2. Update `nerdctl/provider.go`:
   - The `node()` method (line ~277) must change from `return &node{binaryName: p.binaryName, name: name}` to `return &common.Node{Name: name, BinaryName: p.binaryName}`.
   - Ensure `common` import exists (it already does).

3. Update `podman/provider.go`:
   - The `node()` method (line ~312) must change from `return &node{name: name}` to `return &common.Node{Name: name, BinaryName: "podman"}`.
   - Ensure `common` import exists (it already does).

4. Delete the three per-provider node.go files:
   - Delete `pkg/cluster/internal/providers/docker/node.go`
   - Delete `pkg/cluster/internal/providers/nerdctl/node.go`
   - Delete `pkg/cluster/internal/providers/podman/node.go`

5. Update `go.mod`:
   - Change `go 1.17` to `go 1.21.0`
   - Add `toolchain go1.26.0` on the line immediately after the `go` directive
   - Do NOT change `.go-version` file (it controls CI pinning, not go.mod)
   - Do NOT add any new dependencies
   - Run `go mod tidy` to ensure go.sum is consistent

6. Verify everything compiles and tests pass:
   - Run `go build ./...` to confirm all packages compile
   - Run `go test ./pkg/cluster/internal/providers/...` to confirm provider tests pass
   - Run `go test ./...` as final validation
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && go build ./... && go test ./pkg/cluster/internal/providers/...</automated>
  </verify>
  <done>All three provider.go files use common.Node, all three per-provider node.go files are deleted, go.mod has go 1.21.0 with toolchain go1.26.0, all packages compile, all provider tests pass</done>
</task>

</tasks>

<verification>
1. `go build ./...` passes with zero errors
2. `go test ./...` passes with zero failures
3. `ls pkg/cluster/internal/providers/docker/node.go` returns "No such file"
4. `ls pkg/cluster/internal/providers/nerdctl/node.go` returns "No such file"
5. `ls pkg/cluster/internal/providers/podman/node.go` returns "No such file"
6. `grep "go 1.21.0" go.mod` finds the updated directive
7. `grep "toolchain go1.26.0" go.mod` finds the toolchain directive
8. `grep "NodeRoleLabelKey" pkg/cluster/internal/providers/common/constants.go` confirms export
9. `grep "common.Node" pkg/cluster/internal/providers/docker/provider.go` confirms factory update
</verification>

<success_criteria>
- common/node.go exists with exported Node struct parameterized by BinaryName
- nodeRoleLabelKey exported as NodeRoleLabelKey in common/constants.go
- All 3 provider.go files use common.Node in their node() factory
- docker/node.go, nerdctl/node.go, podman/node.go are deleted
- go.mod has go 1.21.0 and toolchain go1.26.0
- go build ./... and go test ./... pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/20-provider-code-deduplication/20-01-SUMMARY.md`
</output>
