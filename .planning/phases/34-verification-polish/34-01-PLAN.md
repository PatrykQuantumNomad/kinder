---
phase: 34-verification-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kinder-site/src/content/docs/changelog.md
  - kinder-site/src/content/docs/installation.md
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
  - GUIDE-03

must_haves:
  truths:
    - "All internal links across every page resolve without 404s"
    - "Production build completes with zero errors and 19 pages built"
    - "All 19 pages are reachable from the sidebar navigation"
    - "CLI commands in docs match what the Go source code defines"
    - "The ci profile description is consistent across all pages"
    - "The Go version requirement on the installation page matches go.mod"
  artifacts:
    - path: "kinder-site/src/content/docs/changelog.md"
      provides: "Fixed ci profile description"
      contains: "Metrics Server + cert-manager"
    - path: "kinder-site/src/content/docs/installation.md"
      provides: "Corrected Go version requirement"
      contains: "Go 1.24"
  key_links:
    - from: "kinder-site/astro.config.mjs"
      to: "kinder-site/src/content/docs/**/*.md"
      via: "sidebar slug definitions"
      pattern: "slug: '"
    - from: "kinder-site/src/content/docs/changelog.md"
      to: "pkg/cluster/createoption.go"
      via: "ci profile description must match source"
      pattern: "ci.*Metrics Server.*cert-manager"
---

<objective>
Full site audit, link check, content accuracy fixes, and production build verification for the kinder documentation site.

Purpose: Close v1.5 milestone by ensuring all 19 pages are consistent, all internal links resolve, CLI commands in docs match Go source code, and the production build is clean. This is the final quality gate before shipping.
Output: Fixed changelog.md and installation.md content bugs, verified clean production build with 19 pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-verification-polish/34-RESEARCH.md
@kinder-site/astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix confirmed content bugs and audit all pages for accuracy</name>
  <files>kinder-site/src/content/docs/changelog.md, kinder-site/src/content/docs/installation.md</files>
  <action>
Fix the two confirmed content bugs, then run a comprehensive audit of all 19 pages.

**Bug Fix 1 — changelog.md line 49:**
Change `ci — MetalLB + Metrics Server (CI-optimized)` to `ci — Metrics Server + cert-manager (CI-optimized)`.
The source of truth is `pkg/cluster/createoption.go:155` where the `ci` case sets `MetricsServer: true, CertManager: true` (no MetalLB).

**Bug Fix 2 — installation.md:**
Change "Go 1.25.7 or later" to "Go 1.24 or later". The go.mod file specifies `go 1.24.0` as the minimum language version. The `.go-version` file (1.25.7) is the compiler version used for binary builds, but 1.24 is the actual minimum requirement for compilation. Users with Go 1.24+ can build kinder.

**Audit checks (grep-based, do NOT install external tools):**

1. **Sidebar completeness** — Count sidebar slugs in `kinder-site/astro.config.mjs` and confirm each has a corresponding `.md`/`.mdx` file under `kinder-site/src/content/docs/`. There should be exactly 18 content files mapping to 19 sidebar entries (index.mdx is the homepage, not a sidebar slug).

2. **Internal link scan** — Run `grep -rn "\[.*\](/[^h]" kinder-site/src/content/docs/` to find all internal markdown links. For each link target found, confirm a corresponding content file exists. Expected targets: `/installation`, `/configuration`, `/addons/*`, `/guides/*`, `/cli-reference/*`.

3. **Placeholder scan** — Run `grep -rni "coming soon\|placeholder\|TODO\|FIXME\|stub\|lorem ipsum" kinder-site/src/content/docs/`. Expected: zero results after v1.5 phases 30-33 filled all placeholders. If any "Coming soon" callouts remain in pages that should now have content, remove them.

4. **CLI accuracy cross-check** — Verify these claims match Go source:
   - `--profile` values (minimal, full, gateway, ci) in `pkg/cluster/createoption.go`
   - `kinder env` JSON field names in `pkg/cmd/kind/env/env.go`
   - Default node image in `pkg/apis/config/defaults/image.go`
   - Compare against docs: `quick-start.md`, `cli-reference/profile-comparison.md`, `cli-reference/json-output.md`, `cli-reference/troubleshooting.md`

5. **Content consistency** — Confirm the `ci` profile is described identically in `changelog.md`, `quick-start.md`, and `cli-reference/profile-comparison.md` after the fix.

**Important:** Do NOT update version numbers in Expected output blocks within tutorials (e.g., `kindest/node:v1.32.0` in `tls-web-app.md`). These are illustrative samples, not live-scraped values. Only fix command syntax, flag names, and JSON field names if they mismatch the source.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder && grep "ci —" kinder-site/src/content/docs/changelog.md | grep -q "cert-manager" && grep "Go 1.24" kinder-site/src/content/docs/installation.md | grep -q "or later" && test $(grep -rnic "coming soon\|placeholder\|TODO\|FIXME\|stub\|lorem ipsum" kinder-site/src/content/docs/ | awk -F: '{s+=$2}END{print s}') -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - changelog.md ci profile line reads "Metrics Server + cert-manager" (not "MetalLB + Metrics Server")
    - installation.md says "Go 1.24 or later" (not "Go 1.25.7")
    - Zero placeholder/TODO/FIXME content found across all 19 pages
    - All internal markdown links point to existing pages
    - CLI commands in docs match Go source definitions
  </done>
</task>

<task type="auto">
  <name>Task 2: Production build verification and sidebar reachability check</name>
  <files></files>
  <action>
Run the production build as the final gate, then verify sidebar completeness.

**Step 1 — Clean build:**
```bash
cd kinder-site && npm run build
```
The build must complete with:
- Zero errors
- Zero warnings about broken links or missing slugs
- Exactly 19 pages listed in the build output

**Step 2 — Sidebar-to-build cross-check:**
Count sidebar entries in `astro.config.mjs`:
- 3 top-level pages: installation, quick-start, configuration
- 7 addon pages: metallb, envoy-gateway, metrics-server, coredns, headlamp, local-registry, cert-manager
- 3 guide pages: tls-web-app, hpa-auto-scaling, local-dev-workflow
- 3 CLI reference pages: profile-comparison, json-output, troubleshooting
- 1 changelog
- 1 index (homepage)
Total: 19 pages

Verify each slug in the sidebar config has a corresponding HTML file in the `dist/` output directory.

**Step 3 — Final link grep:**
After the build, run one more internal link scan to confirm nothing was accidentally broken by the edits in Task 1:
```bash
grep -rn "\[.*\](/[^h]" kinder-site/src/content/docs/
```
Every target must still resolve to a real page.

If the build fails or page count differs from 19, investigate and fix the root cause before marking complete.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder/kinder-site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build` exits with code 0
    - Build output shows exactly 19 pages built
    - No errors or broken-link warnings in build output
    - Every sidebar slug maps to a built HTML page in dist/
  </done>
</task>

</tasks>

<verification>
Phase 34 success criteria (all four from ROADMAP):

1. **All internal links resolve without 404s** — Verified by `npm run build` (Astro validates slug references at build time) plus grep scan of markdown links confirming all targets have corresponding source files.

2. **Production build completes with zero errors and zero broken link warnings** — Verified by running `npm run build` in `kinder-site/` as the final step. Must show 19 pages, zero errors.

3. **All new pages are reachable from the sidebar navigation** — Verified by cross-checking the 19 sidebar slugs in `astro.config.mjs` against the 18 content files + 1 index.mdx. Build passing confirms all slugs resolve.

4. **Code blocks in tutorials are syntactically correct and commands match what the CLI actually accepts** — Verified by grep cross-check of `--profile` values, `kinder env` fields, and default image against Go source files `createoption.go`, `env.go`, and `image.go`.
</verification>

<success_criteria>
- Production build (`npm run build`) passes with zero errors and 19 pages
- changelog.md ci profile bug is fixed (says "Metrics Server + cert-manager")
- installation.md Go version matches go.mod minimum (says "Go 1.24 or later")
- Zero placeholder/TODO/FIXME content remains
- All internal markdown links resolve to existing pages
- CLI claims in docs verified against Go source code
</success_criteria>

<output>
After completion, create `.planning/phases/34-verification-polish/34-01-SUMMARY.md`
</output>
