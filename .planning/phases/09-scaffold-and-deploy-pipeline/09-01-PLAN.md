---
phase: 09-scaffold-and-deploy-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kinder-site/astro.config.mjs
  - kinder-site/package.json
  - kinder-site/package-lock.json
  - kinder-site/tsconfig.json
  - kinder-site/src/content/docs/index.mdx
  - kinder-site/src/env.d.ts
  - kinder-site/public/CNAME
  - kinder-site/public/.nojekyll
  - .github/workflows/deploy-site.yml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "kinder-site/ contains a buildable Astro/Starlight project"
    - "npm run build in kinder-site/ produces dist/ with CNAME, .nojekyll, and no _astro/ stripping issues"
    - "astro.config.mjs sets site to https://kinder.patrykgolabek.dev with no base setting"
    - "deploy-site.yml triggers on kinder-site/** changes and does NOT trigger on Go code changes"
    - "deploy-site.yml deploys only on push to main, not on PRs"
  artifacts:
    - path: "kinder-site/astro.config.mjs"
      provides: "Astro + Starlight config with site URL and GitHub social link"
      contains: "site: 'https://kinder.patrykgolabek.dev'"
    - path: "kinder-site/package.json"
      provides: "Node.js project with astro and @astrojs/starlight dependencies"
      contains: "@astrojs/starlight"
    - path: "kinder-site/public/CNAME"
      provides: "Custom domain for GitHub Pages"
      contains: "kinder.patrykgolabek.dev"
    - path: "kinder-site/public/.nojekyll"
      provides: "Jekyll suppression for _astro/ assets"
    - path: ".github/workflows/deploy-site.yml"
      provides: "CI/CD pipeline for site build and deploy"
      contains: "path: kinder-site"
  key_links:
    - from: ".github/workflows/deploy-site.yml"
      to: "kinder-site/"
      via: "withastro/action path input"
      pattern: "path: kinder-site"
    - from: ".github/workflows/deploy-site.yml"
      to: "kinder-site/**"
      via: "paths filter on push/PR triggers"
      pattern: "paths:.*kinder-site"
    - from: "kinder-site/astro.config.mjs"
      to: "https://kinder.patrykgolabek.dev"
      via: "site config property"
      pattern: "site:.*kinder\\.patrykgolabek\\.dev"
    - from: "kinder-site/public/CNAME"
      to: "GitHub Pages custom domain"
      via: "CNAME file in dist/"
      pattern: "kinder\\.patrykgolabek\\.dev"
---

<objective>
Scaffold an Astro/Starlight project in kinder-site/ and create the GitHub Actions deploy workflow, establishing all file-level infrastructure for the kinder website.

Purpose: Create the complete project scaffolding and CI/CD pipeline so that once DNS and GitHub Pages are configured (Plan 02), the site deploys automatically on push to main.
Output: Buildable Astro/Starlight project in kinder-site/, GitHub Actions workflow in .github/workflows/deploy-site.yml
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scaffold-and-deploy-pipeline/09-RESEARCH.md
@.planning/phases/09-scaffold-and-deploy-pipeline/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro/Starlight project with custom domain files</name>
  <files>
    kinder-site/astro.config.mjs
    kinder-site/package.json
    kinder-site/package-lock.json
    kinder-site/tsconfig.json
    kinder-site/src/content/docs/index.mdx
    kinder-site/src/env.d.ts
    kinder-site/public/CNAME
    kinder-site/public/.nojekyll
  </files>
  <action>
    1. From the repo root, run the Astro scaffold command:
       ```
       npm create astro@latest -- --template starlight kinder-site
       ```
       When prompted:
       - Install dependencies: Yes
       - Initialize git repo: No (already in a git repo)
       - TypeScript: Strict

    2. After scaffolding completes, replace `kinder-site/astro.config.mjs` with the following exact content:
       ```javascript
       import { defineConfig } from 'astro/config';
       import starlight from '@astrojs/starlight';

       export default defineConfig({
         site: 'https://kinder.patrykgolabek.dev',
         integrations: [
           starlight({
             title: 'kinder',
             social: [
               { icon: 'github', label: 'GitHub', href: 'https://github.com/patrykattc/kinder' },
             ],
           }),
         ],
       });
       ```
       CRITICAL: Do NOT set `base`. Custom domain serves from root — setting `base` breaks all asset paths.
       Per user decision: title is "kinder" (lowercase), GitHub link uses patrykattc/kinder (confirmed correct repo), sidebar uses Starlight defaults, landing page is Starlight default splash.

    3. Create `kinder-site/public/CNAME` with exactly one line:
       ```
       kinder.patrykgolabek.dev
       ```
       No trailing newline needed. Astro copies public/ to dist/ on build, so this ends up in dist/CNAME for GitHub Pages.

    4. Create `kinder-site/public/.nojekyll` as an empty file:
       ```
       touch kinder-site/public/.nojekyll
       ```
       This prevents GitHub Pages Jekyll processing from stripping _astro/ directories. Although withastro/action artifact deploys skip Jekyll, this file satisfies the success criterion check for dist/.nojekyll.

    5. Verify the project builds successfully:
       ```
       cd kinder-site && npm run build
       ```

    6. Verify dist/ output contains the expected files:
       - dist/CNAME exists and contains "kinder.patrykgolabek.dev"
       - dist/.nojekyll exists
       - dist/ contains HTML files (index.html at minimum)
       - No base path prefix in generated HTML links (grep for href="/kinder" should return nothing)

    Package manager choice: npm (per research recommendation — simpler than pnpm, withastro/action auto-detects package-lock.json, no .npmrc workarounds needed).
    Node.js version: v22 LTS (enforced in CI via workflow, locally use whatever is installed as long as it meets Astro's minimum of v18.20.8).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder/kinder-site && npm run build && test -f dist/CNAME && test -f dist/.nojekyll && grep -q "kinder.patrykgolabek.dev" dist/CNAME && test -f dist/index.html && ! grep -r 'href="/kinder/' dist/index.html</automated>
  </verify>
  <done>
    - kinder-site/ contains a complete Astro/Starlight project that builds without errors
    - astro.config.mjs has site set to https://kinder.patrykgolabek.dev, no base setting, title "kinder", GitHub social link to patrykattc/kinder
    - dist/CNAME contains "kinder.patrykgolabek.dev"
    - dist/.nojekyll exists (empty file)
    - dist/index.html exists with no base path prefix in links
    - package-lock.json is committed (required by withastro/action for lockfile detection)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy-site.yml</files>
  <action>
    Create `.github/workflows/deploy-site.yml` with the following exact content:

    ```yaml
    name: Deploy Site

    on:
      push:
        branches: [main]
        paths:
          - 'kinder-site/**'
          - '.github/workflows/deploy-site.yml'
      pull_request:
        branches: [main]
        paths:
          - 'kinder-site/**'
          - '.github/workflows/deploy-site.yml'

    permissions:
      contents: read
      pages: write
      id-token: write

    jobs:
      build:
        name: Build
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v5
          - name: Build and upload site artifact
            uses: withastro/action@v5
            with:
              path: kinder-site
              node-version: '22'

      deploy:
        name: Deploy
        needs: build
        runs-on: ubuntu-latest
        if: github.event_name == 'push'
        environment:
          name: github-pages
          url: ${{ steps.deployment.outputs.page_url }}
        steps:
          - name: Deploy to GitHub Pages
            id: deployment
            uses: actions/deploy-pages@v4
    ```

    Per user decisions:
    - Deploy on push to main only (deploy job has `if: github.event_name == 'push'`)
    - Build check runs on PRs (pull_request trigger present, deploy job skipped)
    - Path filter includes `kinder-site/**` AND `.github/workflows/deploy-site.yml` (workflow self-modification triggers rebuild)
    - Go code changes do NOT trigger this workflow (no paths outside kinder-site/ and this workflow file)
    - Node.js v22 LTS pinned in workflow via `node-version: '22'`
    - withastro/action@v5 with `path: kinder-site` for monorepo support

    Note: The `paths:` filter in CONTEXT.md mentions `.github/workflows/site*` as a pattern. The research recommends the specific filename `.github/workflows/deploy-site.yml` which matches the `site*` glob pattern. Using the exact filename is more explicit and correct.
  </action>
  <verify>
    <automated>test -f /Users/patrykattc/work/git/kinder/.github/workflows/deploy-site.yml && grep -q "path: kinder-site" /Users/patrykattc/work/git/kinder/.github/workflows/deploy-site.yml && grep -q "node-version: '22'" /Users/patrykattc/work/git/kinder/.github/workflows/deploy-site.yml && grep -q "github.event_name == 'push'" /Users/patrykattc/work/git/kinder/.github/workflows/deploy-site.yml && grep -q "actions/deploy-pages@v4" /Users/patrykattc/work/git/kinder/.github/workflows/deploy-site.yml</automated>
  </verify>
  <done>
    - .github/workflows/deploy-site.yml exists with two-job structure (build + deploy)
    - Triggers on push to main AND pull_request to main, both path-filtered to kinder-site/** and the workflow file itself
    - Deploy job only runs on push (not PRs) via if condition
    - withastro/action@v5 configured with path: kinder-site and node-version: '22'
    - actions/deploy-pages@v4 handles the actual GitHub Pages deployment
    - Proper permissions set (contents: read, pages: write, id-token: write)
  </done>
</task>

</tasks>

<verification>
1. `cd kinder-site && npm run build` completes without errors
2. `ls kinder-site/dist/CNAME kinder-site/dist/.nojekyll kinder-site/dist/index.html` — all three files exist
3. `grep "kinder.patrykgolabek.dev" kinder-site/dist/CNAME` — custom domain present
4. `grep "site:" kinder-site/astro.config.mjs` — contains https://kinder.patrykgolabek.dev
5. `grep "base:" kinder-site/astro.config.mjs` — returns nothing (no base setting)
6. `grep "path: kinder-site" .github/workflows/deploy-site.yml` — monorepo path configured
7. `grep "github.event_name == 'push'" .github/workflows/deploy-site.yml` — deploy gated to push only
</verification>

<success_criteria>
- Astro/Starlight project in kinder-site/ builds successfully with `npm run build`
- dist/ output contains CNAME, .nojekyll, and HTML pages with no base path prefix
- GitHub Actions workflow is correctly configured for monorepo path filtering and push-only deployment
- All files are committed and ready for the DNS/GitHub Pages setup in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/09-scaffold-and-deploy-pipeline/09-01-SUMMARY.md`
</output>
