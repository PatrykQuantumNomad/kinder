---
phase: 43-air-gapped-cluster-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/internal/apis/config/types.go
  - pkg/apis/config/v1alpha4/types.go
  - pkg/internal/apis/config/convert_v1alpha4.go
  - pkg/cluster/createoption.go
  - pkg/cluster/internal/create/create.go
  - pkg/cmd/kind/create/cluster/createcluster.go
  - pkg/cluster/internal/create/actions/installmetallb/metallb.go
  - pkg/cluster/internal/create/actions/installmetricsserver/metricsserver.go
  - pkg/cluster/internal/create/actions/installcertmanager/certmanager.go
  - pkg/cluster/internal/create/actions/installenvoygw/envoygw.go
  - pkg/cluster/internal/create/actions/installdashboard/dashboard.go
  - pkg/cluster/internal/create/actions/installlocalregistry/localregistry.go
  - pkg/cluster/internal/create/actions/installnvidiagpu/nvidiagpu.go
  - pkg/cluster/internal/providers/common/images.go
  - pkg/cluster/internal/providers/common/images_test.go
autonomous: true
requirements: [AIRGAP-01, AIRGAP-03, AIRGAP-04]

must_haves:
  truths:
    - "AirGapped bool field exists on internal config.Cluster and is propagated from CLI --air-gapped flag through CreateOption to ClusterOptions to config"
    - "Each addon action package exports a var Images []string containing its exact image references"
    - "RequiredAddonImages(cfg) returns the union of all enabled addons' images plus LB image when applicable"
    - "go build ./... succeeds with no import cycle errors"
  artifacts:
    - path: "pkg/internal/apis/config/types.go"
      provides: "AirGapped bool field on Cluster struct"
      contains: "AirGapped"
    - path: "pkg/cluster/createoption.go"
      provides: "CreateWithAirGapped option function"
      contains: "CreateWithAirGapped"
    - path: "pkg/cmd/kind/create/cluster/createcluster.go"
      provides: "--air-gapped CLI flag wired to CreateWithAirGapped"
      contains: "air-gapped"
    - path: "pkg/cluster/internal/providers/common/images.go"
      provides: "RequiredAddonImages and RequiredAllImages functions"
      contains: "RequiredAddonImages"
    - path: "pkg/cluster/internal/providers/common/images_test.go"
      provides: "Tests for RequiredAddonImages and RequiredAllImages with various addon combos"
      contains: "TestRequiredAddonImages"
  key_links:
    - from: "pkg/cmd/kind/create/cluster/createcluster.go"
      to: "pkg/cluster/createoption.go"
      via: "CLI flag calls CreateWithAirGapped(flags.AirGapped)"
      pattern: "CreateWithAirGapped"
    - from: "pkg/cluster/createoption.go"
      to: "pkg/cluster/internal/create/create.go"
      via: "sets ClusterOptions.AirGapped which is copied to Config.AirGapped"
      pattern: "AirGapped"
    - from: "pkg/cluster/internal/providers/common/images.go"
      to: "pkg/cluster/internal/create/actions/installmetallb"
      via: "imports addon Images vars to build complete image set"
      pattern: "installmetallb\\.Images"
---

<objective>
Add the AirGapped flag plumbing from CLI to internal config, export addon image constants from each addon package, and build the RequiredAddonImages/RequiredAllImages utility in common/images.go.

Purpose: This is the foundation layer for air-gapped cluster creation. All subsequent plans (provider fast-fail, doctor check, documentation) depend on the AirGapped config field and the centralized addon image list.

Output: AirGapped field on config.Cluster, --air-gapped CLI flag, CreateWithAirGapped option, addon Image vars, RequiredAddonImages utility with tests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-air-gapped-cluster-creation/43-RESEARCH.md

@pkg/internal/apis/config/types.go
@pkg/apis/config/v1alpha4/types.go
@pkg/internal/apis/config/convert_v1alpha4.go
@pkg/cluster/createoption.go
@pkg/cluster/internal/create/create.go
@pkg/cmd/kind/create/cluster/createcluster.go
@pkg/cluster/internal/providers/common/images.go
@pkg/cluster/internal/loadbalancer/const.go
@pkg/cluster/internal/create/actions/installlocalregistry/localregistry.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AirGapped field to config types and wire CLI flag through CreateOption</name>
  <files>
    pkg/internal/apis/config/types.go
    pkg/apis/config/v1alpha4/types.go
    pkg/internal/apis/config/convert_v1alpha4.go
    pkg/cluster/createoption.go
    pkg/cluster/internal/create/create.go
    pkg/cmd/kind/create/cluster/createcluster.go
  </files>
  <action>
    1. In `pkg/internal/apis/config/types.go`, add `AirGapped bool` field to the `Cluster` struct, after the `Addons` field. Add comment: `// AirGapped disables image pulls; cluster creation fails fast if required images are missing locally.`

    2. In `pkg/apis/config/v1alpha4/types.go`, add `AirGapped bool` to the public `Cluster` struct with yaml/json tags: `AirGapped bool \`yaml:"airGapped,omitempty" json:"airGapped,omitempty"\``. Place after the existing `Addons` field.

    3. In `pkg/internal/apis/config/convert_v1alpha4.go`, in the `Convertv1alpha4` function (which handles v1alpha4 -> internal conversion), propagate the AirGapped field by adding `out.AirGapped = in.AirGapped` after the `out.Addons = Addons{...}` block (where `in` is v1alpha4.Cluster and `out` is the internal config.Cluster). This follows the same pattern used for Addons, FeatureGates, and Networking.

    4. In `pkg/cluster/internal/create/create.go`, add `AirGapped bool` to `ClusterOptions` struct. In `fixupOptions`, after existing fixup logic, add: `if opts.AirGapped { opts.Config.AirGapped = true }` — this ensures CLI flag overrides any config file value.

    5. In `pkg/cluster/createoption.go`, add the `CreateWithAirGapped` function following the exact pattern of `CreateWithRetain`:
    ```go
    func CreateWithAirGapped(airGapped bool) CreateOption {
        return createOptionAdapter(func(o *internalcreate.ClusterOptions) error {
            o.AirGapped = airGapped
            return nil
        })
    }
    ```

    6. In `pkg/cmd/kind/create/cluster/createcluster.go`:
       - Add `AirGapped bool` to the `flagpole` struct.
       - Add the flag registration in `NewCommand`: `cmd.Flags().BoolVar(&flags.AirGapped, "air-gapped", false, "fail fast if required images are missing locally instead of pulling")`
       - In `runE`, add `cluster.CreateWithAirGapped(flags.AirGapped)` to the `provider.Create()` call, after the profile option.

    7. Run `go build ./...` to verify no compilation errors.
  </action>
  <verify>
    `cd /Users/patrykattc/work/git/kinder && go build ./...` succeeds.
    `grep -n 'AirGapped' pkg/internal/apis/config/types.go` shows the new field.
    `grep -n 'air-gapped' pkg/cmd/kind/create/cluster/createcluster.go` shows the CLI flag.
  </verify>
  <done>
    AirGapped bool flows from --air-gapped CLI flag through CreateWithAirGapped -> ClusterOptions.AirGapped -> config.Cluster.AirGapped. The field is also settable via v1alpha4 config YAML (airGapped: true). go build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Export addon image constants and build RequiredAddonImages/RequiredAllImages utility</name>
  <files>
    pkg/cluster/internal/create/actions/installmetallb/metallb.go
    pkg/cluster/internal/create/actions/installmetricsserver/metricsserver.go
    pkg/cluster/internal/create/actions/installcertmanager/certmanager.go
    pkg/cluster/internal/create/actions/installenvoygw/envoygw.go
    pkg/cluster/internal/create/actions/installdashboard/dashboard.go
    pkg/cluster/internal/create/actions/installlocalregistry/localregistry.go
    pkg/cluster/internal/create/actions/installnvidiagpu/nvidiagpu.go
    pkg/cluster/internal/providers/common/images.go
    pkg/cluster/internal/providers/common/images_test.go
  </files>
  <action>
    1. In each addon action package, add an exported `var Images = []string{...}` containing the exact image references used in the embedded manifests. Find the actual image references by grepping the embedded YAML manifests or the Go const/var that references them. Specifically:

       - `installmetallb/metallb.go`: `var Images = []string{"quay.io/metallb/controller:v0.15.3", "quay.io/metallb/speaker:v0.15.3"}`
       - `installmetricsserver/metricsserver.go`: `var Images = []string{"registry.k8s.io/metrics-server/metrics-server:v0.8.1"}`
       - `installcertmanager/certmanager.go`: `var Images = []string{"quay.io/jetstack/cert-manager-cainjector:v1.16.3", "quay.io/jetstack/cert-manager-controller:v1.16.3", "quay.io/jetstack/cert-manager-webhook:v1.16.3"}`
       - `installenvoygw/envoygw.go`: `var Images = []string{"docker.io/envoyproxy/ratelimit:ae4cee11", "envoyproxy/gateway:v1.3.1"}` — verify exact tags from the embedded install.yaml manifest
       - `installdashboard/dashboard.go`: `var Images = []string{"ghcr.io/headlamp-k8s/headlamp:v0.40.1"}` — verify exact tag from the embedded headlamp.yaml manifest
       - `installlocalregistry/localregistry.go`: Export the existing `registryImage` const as `var Images = []string{registryImage}` (keep the existing private const, just reference it)
       - `installnvidiagpu/nvidiagpu.go`: `var Images = []string{"nvcr.io/nvidia/k8s-device-plugin:v0.17.1"}` — verify exact tag from embedded manifest

       IMPORTANT: Verify each image reference by grepping the actual embedded YAML files in the manifests/ subdirectory of each addon package. The research lists expected values but the source of truth is the manifest files.

    2. In `pkg/cluster/internal/providers/common/images.go`, add two new functions:

       ```go
       // RequiredAddonImages returns the set of images required by enabled addons.
       // This includes the LB image when the cluster has an implicit load balancer.
       func RequiredAddonImages(cfg *config.Cluster) sets.String {
           images := sets.NewString()
           // LB image is needed for HA clusters
           if config.ClusterHasImplicitLoadBalancer(cfg) {
               images.Insert(loadbalancer.Image)
           }
           if cfg.Addons.LocalRegistry {
               images.Insert(installlocalregistry.Images...)
           }
           if cfg.Addons.MetalLB {
               images.Insert(installmetallb.Images...)
           }
           if cfg.Addons.MetricsServer {
               images.Insert(installmetricsserver.Images...)
           }
           if cfg.Addons.CertManager {
               images.Insert(installcertmanager.Images...)
           }
           if cfg.Addons.EnvoyGateway {
               images.Insert(installenvoygw.Images...)
           }
           if cfg.Addons.Dashboard {
               images.Insert(installdashboard.Images...)
           }
           if cfg.Addons.NvidiaGPU {
               images.Insert(installnvidiagpu.Images...)
           }
           return images
       }

       // RequiredAllImages returns the union of node images and addon images.
       func RequiredAllImages(cfg *config.Cluster) sets.String {
           all := RequiredNodeImages(cfg)
           for _, img := range RequiredAddonImages(cfg).List() {
               all.Insert(img)
           }
           return all
       }
       ```

       Add the necessary imports for the addon packages and `loadbalancer`. CRITICAL: Verify no import cycle exists by checking that NONE of the addon packages import `common`. Grep for `"common"` or `providers/common` in each addon package — they should only import `actions`, `nodeutils`, `errors`, etc. Also import the `loadbalancer` package (`pkg/cluster/internal/loadbalancer`).

    3. Create `pkg/cluster/internal/providers/common/images_test.go` with tests:
       - `TestRequiredAddonImages_AllDisabled`: All addons disabled, single CP -> returns empty set
       - `TestRequiredAddonImages_AllEnabled`: All addons enabled, 3 CPs -> returns all 12 images + LB image
       - `TestRequiredAddonImages_MetalLBOnly`: Only MetalLB enabled -> returns exactly 2 metallb images
       - `TestRequiredAllImages`: Verifies node images + addon images are unioned correctly
       - `TestRequiredAddonImages_LBOnlyWithMultiCP`: No addons enabled, 3 CPs -> returns only LB image

    4. Run `go build ./...` to confirm no import cycles. Run `go test ./pkg/cluster/internal/providers/common/...` to confirm tests pass.
  </action>
  <verify>
    `cd /Users/patrykattc/work/git/kinder && go build ./...` succeeds (no import cycles).
    `cd /Users/patrykattc/work/git/kinder && go test ./pkg/cluster/internal/providers/common/...` passes.
    `grep -rn 'var Images' pkg/cluster/internal/create/actions/*/` shows Images exported from each addon package.
  </verify>
  <done>
    Every addon action package exports `var Images []string` with its exact image references. `RequiredAddonImages(cfg)` returns the correct set based on enabled addons and LB presence. `RequiredAllImages(cfg)` unions node and addon images. All tests pass. No import cycles.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` compiles without errors (no import cycles, no type errors)
2. `go test ./pkg/cluster/internal/providers/common/...` passes all image utility tests
3. `go vet ./...` reports no issues on new code
4. `grep -rn 'var Images' pkg/cluster/internal/create/actions/*/` confirms all 7 addon packages export Images
5. `grep 'AirGapped' pkg/internal/apis/config/types.go pkg/cluster/internal/create/create.go pkg/cluster/createoption.go` confirms field plumbing
</verification>

<success_criteria>
- AirGapped field exists on config.Cluster and is settable via --air-gapped CLI flag and airGapped YAML config field
- All 7 addon packages + LB have their image references exported as Go variables
- RequiredAddonImages correctly computes the image set based on config
- RequiredAllImages unions node images and addon images
- go build and go test pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/43-air-gapped-cluster-creation/43-01-SUMMARY.md`
</output>
