---
phase: 36-homebrew-tap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kinder-site/src/content/docs/installation.md
autonomous: false
requirements:
  - SITE-01

must_haves:
  truths:
    - "Installation page shows Homebrew as the first/preferred macOS install method"
    - "Installation page has correct GoReleaser archive URLs (tar.gz with version in filename) not old cross.sh URLs"
    - "kinder-site builds successfully with no errors after installation.md changes"
  artifacts:
    - path: "kinder-site/src/content/docs/installation.md"
      provides: "Homebrew install instructions and updated binary download URLs"
      contains: "brew install patrykquantumnomad/kinder/kinder"
  key_links:
    - from: "kinder-site/src/content/docs/installation.md"
      to: "https://github.com/PatrykQuantumNomad/kinder/releases"
      via: "download URLs in curl commands"
      pattern: "releases.*download.*kinder_"
---

<objective>
Update the kinder website installation page to add Homebrew as the primary macOS install method and fix all download URLs to match GoReleaser's archive naming convention.

Purpose: Users visiting the installation page will see `brew install` as the easiest way to get kinder on macOS. The manual download URLs are updated from the old cross.sh naming (`kinder-darwin-arm64`, no extension) to GoReleaser's format (`kinder_VERSION_darwin_arm64.tar.gz`).
Output: Updated installation.md with Homebrew section and correct archive URLs
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-homebrew-tap/36-RESEARCH.md
@kinder-site/src/content/docs/installation.md

<interfaces>
<!-- Current installation.md structure -->
From kinder-site/src/content/docs/installation.md:
```markdown
---
title: Installation
description: How to install kinder from a pre-built binary or by building from source.
---
:::note ... :::
## Prerequisites
## Download Pre-built Binary  (has old URLs like kinder-darwin-arm64, no .tar.gz)
### macOS (Apple Silicon)
### macOS (Intel)
### Linux (amd64)
### Linux (arm64)
### Windows (amd64)
## Build from Source
## Verify Installation
```

<!-- GoReleaser archive naming convention (from Phase 35 .goreleaser.yaml) -->
Archive name template: `{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}`
Produces: `kinder_VERSION_darwin_arm64.tar.gz`, `kinder_VERSION_darwin_amd64.tar.gz`, etc.
Windows: `kinder_VERSION_windows_amd64.zip`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Homebrew section and update download URLs in installation.md</name>
  <files>kinder-site/src/content/docs/installation.md</files>
  <action>
Rewrite `kinder-site/src/content/docs/installation.md` with these changes:

1. Keep the existing frontmatter (title, description) and the note callout and Prerequisites section unchanged.

2. Add a NEW section "Homebrew (macOS)" BEFORE the "Download Pre-built Binary" section. This becomes the first install method:

```markdown
## Homebrew (macOS)

```sh
brew install patrykquantumnomad/kinder/kinder
```

This installs a pre-built binary for your architecture (Apple Silicon or Intel). No Go toolchain required.
```

3. Update the "Download Pre-built Binary" section. Change the intro to link to GitHub Releases. Replace ALL curl commands with GoReleaser archive URLs. The old URLs used bare binary names (`kinder-darwin-arm64`), but GoReleaser produces tar.gz archives (`kinder_VERSION_darwin_arm64.tar.gz`).

Since the filename includes the version and `releases/latest/download/` requires the exact filename, the simplest approach is to direct users to the releases page to download. Use this pattern for each platform:

```markdown
## Download Pre-built Binary

Pre-built binaries for macOS, Linux, and Windows are available from
[GitHub Releases](https://github.com/PatrykQuantumNomad/kinder/releases/latest).

Download the archive for your platform, extract, and move to your PATH:

### macOS / Linux

```sh
# Replace the URL with the archive for your platform from the latest release
tar xzf kinder_*_darwin_arm64.tar.gz
chmod +x kinder
sudo mv kinder /usr/local/bin/
```

### Windows

Download the `.zip` archive from [GitHub Releases](https://github.com/PatrykQuantumNomad/kinder/releases/latest) and extract `kinder.exe` to a directory in your `PATH`.
```

IMPORTANT: Do NOT provide hardcoded curl download URLs with version numbers — they become stale. Direct users to the releases page instead.
IMPORTANT: Keep the "Build from Source" and "Verify Installation" sections unchanged.

4. The final section order should be:
   - Frontmatter + note + Prerequisites (unchanged)
   - Homebrew (macOS) (NEW)
   - Download Pre-built Binary (UPDATED — simplified, no per-platform curl with hardcoded URLs)
   - Build from Source (unchanged)
   - Verify Installation (unchanged)
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder/kinder-site && npm run build 2>&1 | tail -5 && grep -c 'brew install' src/content/docs/installation.md && grep -c 'kinder-darwin-arm64' src/content/docs/installation.md</automated>
  </verify>
  <done>
    - installation.md has "Homebrew (macOS)" section with `brew install patrykquantumnomad/kinder/kinder`
    - Zero references to old cross.sh archive names (kinder-darwin-arm64, kinder-linux-amd64, etc.)
    - Download section directs to GitHub Releases page instead of hardcoded versioned URLs
    - kinder-site builds successfully with `npm run build`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify installation page content</name>
  <what-built>Updated installation page with Homebrew as primary macOS method and corrected download URLs</what-built>
  <how-to-verify>
1. Run `cd /Users/patrykattc/work/git/kinder/kinder-site && npm run dev`
2. Visit http://localhost:4321/installation/
3. Verify: "Homebrew (macOS)" section appears BEFORE "Download Pre-built Binary"
4. Verify: The brew command shows `brew install patrykquantumnomad/kinder/kinder`
5. Verify: No old URLs like `kinder-darwin-arm64` (without .tar.gz) appear
6. Verify: Download section links to GitHub Releases page
7. Verify: "Build from Source" section is still present and unchanged
8. Verify: "Verify Installation" section is still present
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd kinder-site && npm run build` completes with zero errors
2. `grep 'brew install' src/content/docs/installation.md` returns at least 1 match
3. `grep 'kinder-darwin-arm64' src/content/docs/installation.md` returns 0 matches (old URLs removed)
4. Visual review confirms Homebrew section is prominent
</verification>

<success_criteria>
- Installation page has Homebrew (macOS) as first install method
- `brew install patrykquantumnomad/kinder/kinder` command visible on the page
- No stale pre-GoReleaser download URLs remain
- kinder-site builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/36-homebrew-tap/36-02-SUMMARY.md`
</output>
