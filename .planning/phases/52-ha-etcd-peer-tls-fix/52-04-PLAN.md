---
phase: 52-ha-etcd-peer-tls-fix
plan: 04
type: execute
wave: 4
depends_on: [52-01, 52-02, 52-03]
files_modified:
  - pkg/internal/doctor/resumestrategy.go
  - pkg/internal/doctor/resumestrategy_test.go
  - pkg/internal/doctor/check.go
  - pkg/internal/doctor/check_test.go
autonomous: true
requirements: [LIFE-09]
must_haves:
  truths:
    - "`kinder doctor` lists a new check `ha-resume-strategy` in the Cluster category that reports the resume strategy of the active HA cluster"
    - "On a single-CP cluster (or no cluster present): the check reports skip with a clear message — never warn or fail"
    - "On an HA cluster with all CPs labeled `resume-strategy=ip-pinned`: status=ok, message='resume-strategy: ip-pinned'"
    - "On an HA cluster with all CPs labeled `resume-strategy=cert-regen`: status=warn, message='resume-strategy: cert-regen' with reason explaining the slower-resume tradeoff"
    - "On an HA cluster where the resume-strategy label is ABSENT on any CP (legacy cluster): status=warn, message='resume-strategy: cert-regen (legacy)' — but ONLY when explicitly invoked; doctor stays quiet on legacy clusters during regular runs (CONTEXT.md D-locked)"
    - "On an HA cluster with MIXED labels across CPs (ip-pinned on some, cert-regen on others): status=fail with message 'resume-strategy: mixed' and reason listing which CPs disagree — this is a corruption signal, not legacy"
    - "The doctor count test pins len(allChecks)==26 (live baseline 24 + probe from Plan 52-01 = 25 + ha-resume-strategy from this plan = 26)"
  artifacts:
    - path: "pkg/internal/doctor/resumestrategy.go"
      provides: "haResumeStrategyCheck implementing Check interface"
      contains: "newHAResumeStrategyCheck"
    - path: "pkg/internal/doctor/resumestrategy_test.go"
      provides: "Unit tests for ip-pinned, cert-regen, legacy, mixed, single-CP, no-cluster, indeterminate, multi-cluster branches"
      contains: "TestHAResumeStrategyCheck"
    - path: "pkg/internal/doctor/check.go"
      provides: "newHAResumeStrategyCheck registered in allChecks Cluster category"
      contains: "newHAResumeStrategyCheck()"
    - path: "pkg/internal/doctor/check_test.go"
      provides: "TestAllChecks_CountIs26 + TestAllChecks_IncludesHAResumeStrategy (renamed from TestAllChecks_CountIs25 created in Plan 52-01)"
      contains: "TestAllChecks_CountIs26"
  key_links:
    - from: "pkg/internal/doctor/check.go"
      to: "pkg/internal/doctor/resumestrategy.go"
      via: "newHAResumeStrategyCheck() registered after newClusterResumeReadinessCheck() in Cluster category"
      pattern: "newHAResumeStrategyCheck"
    - from: "pkg/internal/doctor/resumestrategy.go"
      to: "container labels (read via docker inspect --format)"
      via: "filter cluster CPs, inspect resume-strategy label on each, aggregate verdict"
      pattern: "io.x-k8s.kinder.resume-strategy"
---

<objective>
Plan 52-04 closes the phase by exposing the cluster's resume strategy through
`kinder doctor`. This is the second of the two new doctor checks scoped into
Phase 52 by CONTEXT.md (`ipam-probe` from Plan 52-01 was the first).

Per CONTEXT.md locked decisions:
- "Add a new doctor check that exposes the resume-strategy state for an
  existing HA cluster — values: ip-pinned / cert-regen / unsupported. Helps
  users diagnose slow resumes and understand which path their cluster is on."
- "This new check is added in Phase 52 (not deferred to Phase 57)."
- "The doctor count test must be updated for the new check."
- "Doctor stays quiet on legacy clusters" — the check only WARNS when
  explicitly invoked (which is how all doctor checks behave); the warning is
  diagnostic, not a nag during creation/resume flows.

Per CONTEXT.md Deferred Ideas (NOT in scope):
- No `kinder cluster migrate-ha` command.
- No doctor failure on unmigrated legacy clusters (warn only, not fail —
  except for MIXED-label corruption case which is genuine cluster damage).

Per RESEARCH (HIGH confidence):
- Existing pattern: `clusterResumeReadinessCheck` (`resumereadiness.go`)
  already inspects HA cluster CP containers and emits structured Result.
  Reuse the same listing helper (`realListCPNodes` or its equivalent).
- Label is delivered by Plan 52-02 at `docker run` time. This check reads
  it back via `docker inspect --format '{{index .Config.Labels
  "io.x-k8s.kinder.resume-strategy"}}'`.

Live baseline arithmetic (verified):
- `pkg/internal/doctor/check.go` baseline (before any Phase 52 work) =
  **24** registered checks (count entries in the `var allChecks = []Check{}`
  slice; verification command in Task 2's `<action>` block).
- After Plan 52-01 (adds `newIPAMProbeCheck`) → **25**. Plan 52-01 introduces
  `TestAllChecks_CountIs25`.
- After Plan 52-04 (adds `newHAResumeStrategyCheck`) → **26**. This plan
  renames the count test to `TestAllChecks_CountIs26` and bumps the
  asserted constant from 25 to 26.

Out of scope for this plan:
- Any modification to lifecycle.Resume — that's Plan 52-03 territory.
- Probe re-execution — the doctor check just inspects state, doesn't probe.

Output:
- New `pkg/internal/doctor/resumestrategy.go` with the Check implementation.
- Unit tests covering all eight verdict branches (see Branch Inventory below).
- Registration in allChecks; count test renamed to CountIs26 and bumped to 26.

Why `depends_on: [52-01, 52-02, 52-03]`: this plan edits
`pkg/internal/doctor/check.go` and `pkg/internal/doctor/check_test.go` —
the same files Plan 52-01 edits — and Task 2 specifically RENAMES a test
(`TestAllChecks_CountIs25`) that Plan 52-01 introduces. That is a direct
artifact dependency on 52-01. The dependencies on 52-02 and 52-03 are
informational (interface constants, summaries) but not file-level conflicts.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-ha-etcd-peer-tls-fix/52-CONTEXT.md
@.planning/phases/52-ha-etcd-peer-tls-fix/52-RESEARCH.md
@.planning/phases/52-ha-etcd-peer-tls-fix/52-01-SUMMARY.md
@.planning/phases/52-ha-etcd-peer-tls-fix/52-02-SUMMARY.md
@.planning/phases/52-ha-etcd-peer-tls-fix/52-03-SUMMARY.md

@pkg/internal/doctor/resumereadiness.go
@pkg/internal/doctor/check.go

<interfaces>
<!-- Existing types/exports executor needs. -->

From pkg/internal/doctor/check.go (already loaded above):
```go
type Check interface { Name() string; Category() string; Platforms() []string; Run() []Result }
type Result struct { Name, Category, Status, Message, Reason, Fix string }
```

From pkg/internal/doctor/resumereadiness.go (existing, mirror its pattern):
```go
// Existing pattern uses a package-private listCPNodesFn variable to allow
// test injection of a fake container lister. Reuse the SAME helper if it
// already exposes a cluster-CP listing usable by both checks; otherwise
// add a sibling helper named `listClusterCPContainers(binaryName string) ([]string, error)`.
// Do not duplicate inspection logic.
```

From pkg/internal/lifecycle/ippin.go (delivered Plan 52-02):
```go
const ResumeStrategyLabel = "io.x-k8s.kinder.resume-strategy"
const StrategyIPPinned    = "ip-pinned"
const StrategyCertRegen   = "cert-regen"
```

If those constants moved to a neutral package per Plan 52-02 SUMMARY,
import from there. Doctor MUST NOT depend on lifecycle if a cycle results;
in that case, mirror the three string constants in resumestrategy.go with
a comment pointing back to the canonical definition.
</interfaces>

## Branch Inventory (verdicts the check must produce)

The check produces exactly EIGHT distinct verdicts. Each gets its own test:

| # | Verdict | Status | Trigger |
|---|---------|--------|---------|
| 1 | ok | ok | All CPs labeled ip-pinned |
| 2 | warn (cert-regen) | warn | All CPs labeled cert-regen |
| 3 | warn-legacy | warn | All CPs have empty/absent label |
| 4 | fail-mixed | fail | CPs disagree on label value (corruption) |
| 5 | skip-single-cp | skip | Lister returns 1 CP |
| 6 | skip-no-cluster | skip | Lister returns 0 CPs |
| 7 | warn-indeterminate | warn | Inspect on any CP returns non-zero |
| 8 | warn-multi-cluster | warn | Lister returns CPs spanning multiple clusters |

In addition, two NON-VERDICT meta-tests verify metadata invariants:
- TestHAResumeStrategyCheck_CategoryAndName (Name/Category/Platforms shape)
- TestHAResumeStrategyCheck_DoesNotRunProbe (defensive: ensure no `ProbeIPAM` call)

Total tests in resumestrategy_test.go: 8 verdict + 2 meta = 10. Reference
counts in `<done>` and `<success_criteria>` use the verdict count (8).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement haResumeStrategyCheck + tests</name>
  <files>pkg/internal/doctor/resumestrategy.go, pkg/internal/doctor/resumestrategy_test.go</files>
  <behavior>
    Tests in resumestrategy_test.go (RED first) — eight verdict branches +
    two meta tests. All ten map onto the Branch Inventory above.

    Verdict tests (8):
    - TestHAResumeStrategyCheck_NoCluster: lister returns empty slice.
      Status=skip, Message="no kinder cluster detected". (Verdict 6)
    - TestHAResumeStrategyCheck_SingleCP: lister returns 1 CP container.
      Status=skip, Message="non-HA cluster (1 control-plane node)". (Verdict 5)
    - TestHAResumeStrategyCheck_AllIPPinned: 3 CPs, all labels=
      "ip-pinned". Status=ok, Message="resume-strategy: ip-pinned",
      Reason="" (no reason needed for ok). (Verdict 1)
    - TestHAResumeStrategyCheck_AllCertRegen: 3 CPs, all labels=
      "cert-regen". Status=warn, Message="resume-strategy: cert-regen",
      Reason contains "slower resume" or "regenerates etcd peer certs"
      and Fix is empty (no actionable fix — informational). (Verdict 2)
    - TestHAResumeStrategyCheck_LegacyNoLabel: 3 CPs, label is empty
      string on all. Status=warn, Message="resume-strategy: cert-regen
      (legacy)", Reason explains "cluster predates v2.4 IP-pinning" and
      mentions "delete + recreate to migrate". (Verdict 3)
    - TestHAResumeStrategyCheck_Mixed: 3 CPs, cp1=ip-pinned, cp2=cert-regen,
      cp3=ip-pinned. Status=fail, Message="resume-strategy: mixed",
      Reason names the disagreeing nodes (e.g., "cp2=cert-regen,
      cp1+cp3=ip-pinned"). This is the genuine corruption case. (Verdict 4)
    - TestHAResumeStrategyCheck_InspectFails: docker inspect on cp2
      returns non-zero. Status=warn (not fail — could be transient),
      Message="resume-strategy: indeterminate", Reason includes the
      underlying error. (Verdict 7)
    - TestHAResumeStrategyCheck_MultipleClustersDetected: lister returns
      CPs spanning 2 different cluster labels. Status=warn, Message=
      "multiple kinder clusters present", Reason="check kubectl context".
      (Verdict 8)

    Meta tests (2 — invariants, not verdicts):
    - TestHAResumeStrategyCheck_CategoryAndName: Name()=="ha-resume-strategy",
      Category()=="Cluster", Platforms()==nil (all platforms).
    - TestHAResumeStrategyCheck_DoesNotRunProbe: fakeProbeFn (a stub of
      doctor.ProbeIPAM) is wired; the check must NOT call it. The probe
      is a separate check (`ipam-probe` from Plan 52-01); this check is
      pure inspection of existing state.
  </behavior>
  <action>
    Create `pkg/internal/doctor/resumestrategy.go`:

    1. Type and constructor matching existing pattern
       (resumereadiness.go is the closest analog):
       ```
       type haResumeStrategyCheck struct{}
       func newHAResumeStrategyCheck() Check { return &haResumeStrategyCheck{} }
       func (c *haResumeStrategyCheck) Name() string     { return "ha-resume-strategy" }
       func (c *haResumeStrategyCheck) Category() string { return "Cluster" }
       func (c *haResumeStrategyCheck) Platforms() []string { return nil }
       func (c *haResumeStrategyCheck) Run() []Result    { ... }
       ```

    2. Package-private hooks for testability:
       ```
       var resumeStrategyLister = listKinderCPContainers
       var resumeStrategyInspector = inspectLabel
       ```

    3. `listKinderCPContainers(binaryName string) (clusters map[string][]string, err error)`:
       - `binaryName ps -a --filter "label=io.x-k8s.kinder.role=control-plane" --format "{{.Names}}\t{{.Label \"io.x-k8s.kinder.cluster\"}}"`
         (verify the actual label keys against the existing
         resumereadiness.go pattern — reuse them if they differ).
       - Group by cluster label → map[clusterName][]containerName.

    4. `inspectLabel(binaryName, container, labelKey string) (value string, present bool, err error)`:
       - `binaryName inspect --format "{{index .Config.Labels \"<labelKey>\"}}{{if not (index .Config.Labels \"<labelKey>\")}}__ABSENT__{{end}}" <c>`
         (or use a simpler two-part inspect: one for label, one for
         existence — pick whichever is least error-prone in tests).

    5. Run() logic (must produce all eight verdicts from the Branch Inventory):
       a. Resolve runtime via `resolveRuntimeBinary()` (existing helper).
       b. List CPs grouped by cluster.
       c. If no cluster found → skip with "no kinder cluster detected". (V6)
       d. If multiple clusters found → warn "multiple kinder clusters
          present" with reason "check kubectl context". (V8)
       e. For the single cluster case:
          - If len(cps) < 2 → skip with "non-HA cluster (N control-plane node)". (V5)
          - For each CP, inspect the resume-strategy label.
          - Aggregate the values into a set:
            * 1 value, that value == "ip-pinned" → ok. (V1)
            * 1 value, that value == "cert-regen" → warn (not legacy). (V2)
            * 1 value, that value == "" (absent) → warn (legacy). (V3)
            * >1 distinct value → fail (mixed = corruption). (V4)
            * any inspect error → warn (indeterminate). (V7)

    6. Reason text (verbatim — locked here so the SUMMARY can capture it):
       - cert-regen: "Cluster will regenerate etcd peer certs on each resume
         (~40-60s overhead). Switch to ip-pinned by deleting and
         recreating the cluster on a runtime that supports IP pinning."
       - cert-regen (legacy): "Cluster was created before v2.4 IP-pinning
         was available. Delete and recreate to opt into the faster
         ip-pinned resume path."
       - mixed: "Control-plane nodes disagree on resume-strategy
         (cp1=<v1>, cp2=<v2>, ...). This indicates manual label tampering
         or partial migration; cluster state may be inconsistent. Delete
         and recreate."
       - indeterminate: "Could not read resume-strategy label from one or
         more control-plane nodes: <error>. Re-run after the runtime
         daemon recovers."

    Constraints:
    - Per D-locked: doctor stays quiet on legacy clusters during normal
      flows. This check IS the explicit query path; warn-on-explicit-call
      is correct behavior.
    - Reuse `realListCPNodes` / cluster-listing helper if one already
      exists in resumereadiness.go (DO NOT duplicate). If a helper
      doesn't exist with the right shape, add it as a sibling helper in
      this file with a comment explaining why a new helper was needed.
    - DO NOT import `pkg/internal/lifecycle` if it creates a cycle.
      Mirror the three constants locally with a comment.
    - Use the same `errors.Wrapf` / log conventions as the rest of the
      doctor package.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder &amp;&amp; go test ./pkg/internal/doctor/... -run TestHAResumeStrategy -v -count=1 -race</automated>
  </verify>
  <done>
    The check correctly classifies all eight verdict branches per the
    Branch Inventory (ok / warn-cert-regen / warn-legacy / fail-mixed /
    skip-single-cp / skip-no-cluster / warn-indeterminate / warn-multi-cluster).
    Two meta-tests (CategoryAndName, DoesNotRunProbe) also pass. Ten test
    cases in total. No probe invocation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register in allChecks + rename count test to CountIs26</name>
  <files>pkg/internal/doctor/check.go, pkg/internal/doctor/check_test.go</files>
  <behavior>
    Tests in check_test.go (additions/renames):
    - TestAllChecks_IncludesHAResumeStrategy: AllChecks() contains a Check
      whose Name()=="ha-resume-strategy" and Category()=="Cluster".
    - Rename TestAllChecks_CountIs25 (introduced in Plan 52-01) →
      TestAllChecks_CountIs26, and bump the asserted constant from 25 to 26.
      Rationale: live baseline 24 + probe (52-01) = 25 + ha-resume-strategy
      (this plan) = 26.
  </behavior>
  <action>
    1. Edit `pkg/internal/doctor/check.go`:
       - In the `// Category: Cluster (Phase 42 + 47)` block, append
         `newHAResumeStrategyCheck()` immediately after
         `newClusterResumeReadinessCheck()`.
       - Update the comment to `// Category: Cluster (Phase 42 + 47 + 52)`.

    2. Edit `pkg/internal/doctor/check_test.go`:
       - Rename TestAllChecks_CountIs25 → TestAllChecks_CountIs26, bump
         the asserted constant from 25 to 26.
       - Add TestAllChecks_IncludesHAResumeStrategy.

       VERIFY THE BASELINE BEFORE WRITING: from the command
       ```
       awk '/^var allChecks = \[\]Check{/,/^}/' pkg/internal/doctor/check.go \
         | grep -E '^\s+new.+\(\),' | wc -l
       ```
       on the branch this plan starts from (after Plan 52-01 has landed),
       MUST output `25`. After Task 2.1 appends `newHAResumeStrategyCheck()`
       and the test is re-run, the same command MUST output `26`. If either
       check fails, halt — Plan 52-01 may not have landed correctly.

    Per D-locked: "doctor count test must be updated for the new check"
    — this completes that requirement for both Phase 52 additions
    (probe + resume-strategy).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder &amp;&amp; go test ./pkg/internal/doctor/... -run TestAllChecks -v -count=1</automated>
  </verify>
  <done>
    `kinder doctor` lists `ha-resume-strategy` in the Cluster category.
    Count test renamed to TestAllChecks_CountIs26 and pinned at 26. Both
    new Phase 52 checks discoverable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker inspect output (label values) → user-facing diagnostic message | label string is rendered in Result.Message and Result.Reason |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Information Disclosure | label values rendered to terminal | accept | Labels are kinder-internal namespaced values (`ip-pinned`, `cert-regen`); not user secrets |
| T-52-04-02 | Tampering | user manually mutates resume-strategy label to mismatched values | mitigate | Mixed-label case detected as fail with explicit corruption reason; cluster directed to be recreated |
| T-52-04-03 | DoS | inspect on stopped runtime daemon | mitigate | indeterminate verdict (warn) instead of hard error; doctor exits non-zero only on fail, not warn |
</threat_model>

<verification>
- `go test ./pkg/internal/doctor/... -count=1 -race` passes.
- `go build ./...` succeeds.
- `kinder doctor` (manual sanity, optional): lists `ha-resume-strategy`
  with sensible verdict on a real cluster.
- Phase 52 closure: both new checks (`ipam-probe`, `ha-resume-strategy`)
  registered; count test reflects +2 from baseline (24 → 26).
</verification>

<success_criteria>
- [ ] All eight verdict branch tests pass (ok / warn-cert-regen / warn-legacy / fail-mixed / skip-single-cp / skip-no-cluster / warn-indeterminate / warn-multi-cluster).
- [ ] Two meta tests pass (CategoryAndName, DoesNotRunProbe).
- [ ] Check registered in allChecks Cluster category.
- [ ] Count test renamed to TestAllChecks_CountIs26, asserted constant = 26.
- [ ] No probe invocation from this check (it inspects state only).
- [ ] No import cycle.
- [ ] No new Go module dependencies.
</success_criteria>

<output>
Create `.planning/phases/52-ha-etcd-peer-tls-fix/52-04-SUMMARY.md`. MUST record:
- The verbatim Reason strings used in each branch (these become user-facing
  doctor output and should be reviewed in retrospective).
- Whether `realListCPNodes` was reused or a new lister was added (and why).
- Final allChecks count (must be 26) — Phase 53 may add more, so locking
  the value here helps future merge conflict resolution.
- Confirmation that all eight verdict branches plus the two meta tests are present and passing (10 tests total in resumestrategy_test.go).
- Confirmation that the count test rename (CountIs25 → CountIs26) was applied
  and the previous test name no longer appears in the codebase.
</output>
