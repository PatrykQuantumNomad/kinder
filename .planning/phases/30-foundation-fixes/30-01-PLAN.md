---
phase: 30-foundation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kinder-site/src/components/Comparison.astro
  - kinder-site/src/content/docs/index.mdx
  - kinder-site/src/content/docs/quick-start.md
  - kinder-site/src/content/docs/configuration.md
  - kinder-site/astro.config.mjs
  - kinder-site/src/content/docs/guides/tls-web-app.md
  - kinder-site/src/content/docs/guides/hpa-auto-scaling.md
  - kinder-site/src/content/docs/guides/local-dev-workflow.md
  - kinder-site/src/content/docs/cli-reference/profile-comparison.md
  - kinder-site/src/content/docs/cli-reference/json-output.md
  - kinder-site/src/content/docs/cli-reference/troubleshooting.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Landing page Comparison component shows all 7 addons in the kinder column with matching 'No X' entries in the kind column"
    - "Landing page index.mdx description meta mentions all 7 addons including Local Registry and cert-manager"
    - "Landing page addon cards are grouped into Core Addons (MetalLB, Metrics Server, CoreDNS Tuning) and Optional Addons (Envoy Gateway, Headlamp, Local Registry, cert-manager) with separate headings and CardGrid components"
    - "Quick-start page has verification commands and expected output for all 7 addons"
    - "Quick-start page mentions --profile flag in a tip callout after the main create flow"
    - "Quick-start page has a 'Something wrong?' section pointing to kinder doctor"
    - "Configuration page documents all 7 addon fields including localRegistry and certManager"
    - "Configuration page has a full v1alpha4 YAML example at the top with core vs optional grouping"
    - "Sidebar shows collapsible Guides section with 3 placeholder entries and CLI Reference section with 3 placeholder entries"
  artifacts:
    - path: "kinder-site/src/components/Comparison.astro"
      provides: "Updated kind vs kinder comparison with all 7 addons"
      contains: "Local Registry"
    - path: "kinder-site/src/content/docs/index.mdx"
      provides: "Landing page with core/optional addon card grouping and updated description meta"
      contains: "Core Addons"
    - path: "kinder-site/src/content/docs/quick-start.md"
      provides: "Quick-start with all 7 addon verifications, --profile tip, doctor section"
      contains: "--profile"
    - path: "kinder-site/src/content/docs/configuration.md"
      provides: "Configuration reference with all 7 addon fields"
      contains: "localRegistry"
    - path: "kinder-site/astro.config.mjs"
      provides: "Sidebar with Guides and CLI Reference sections"
      contains: "Guides"
    - path: "kinder-site/src/content/docs/guides/tls-web-app.md"
      provides: "Placeholder guide page"
      contains: "Coming soon"
    - path: "kinder-site/src/content/docs/guides/hpa-auto-scaling.md"
      provides: "Placeholder guide page"
      contains: "Coming soon"
    - path: "kinder-site/src/content/docs/guides/local-dev-workflow.md"
      provides: "Placeholder guide page"
      contains: "Coming soon"
    - path: "kinder-site/src/content/docs/cli-reference/profile-comparison.md"
      provides: "Placeholder CLI reference page"
      contains: "Coming soon"
    - path: "kinder-site/src/content/docs/cli-reference/json-output.md"
      provides: "Placeholder CLI reference page"
      contains: "Coming soon"
    - path: "kinder-site/src/content/docs/cli-reference/troubleshooting.md"
      provides: "Placeholder CLI reference page"
      contains: "Coming soon"
  key_links:
    - from: "kinder-site/astro.config.mjs"
      to: "kinder-site/src/content/docs/guides/*.md"
      via: "sidebar slug references"
      pattern: "slug.*guides/"
    - from: "kinder-site/astro.config.mjs"
      to: "kinder-site/src/content/docs/cli-reference/*.md"
      via: "sidebar slug references"
      pattern: "slug.*cli-reference/"
    - from: "kinder-site/src/content/docs/index.mdx"
      to: "kinder-site/src/components/Comparison.astro"
      via: "component import"
      pattern: "import Comparison"
---

<objective>
Update existing kinder website pages (landing, quick-start, configuration) to accurately reflect all 7 addons from v1.3-v1.4, and add Guides and CLI Reference sidebar sections with placeholder pages for future phases.

Purpose: The site currently documents only 5 addons. Users who installed v1.3+ see Local Registry and cert-manager but can't find them in the docs. This plan brings the documentation into parity with the actual CLI.

Output: Updated landing page (with core/optional card grouping), quick-start, configuration, sidebar navigation, and 6 placeholder content files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-foundation-fixes/30-CONTEXT.md
@.planning/phases/30-foundation-fixes/30-RESEARCH.md
@kinder-site/src/components/Comparison.astro
@kinder-site/src/content/docs/index.mdx
@kinder-site/src/content/docs/quick-start.md
@kinder-site/src/content/docs/configuration.md
@kinder-site/astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update landing page (card grouping + meta), Comparison component, and sidebar with placeholder pages</name>
  <files>
    kinder-site/src/components/Comparison.astro,
    kinder-site/src/content/docs/index.mdx,
    kinder-site/astro.config.mjs,
    kinder-site/src/content/docs/guides/tls-web-app.md,
    kinder-site/src/content/docs/guides/hpa-auto-scaling.md,
    kinder-site/src/content/docs/guides/local-dev-workflow.md,
    kinder-site/src/content/docs/cli-reference/profile-comparison.md,
    kinder-site/src/content/docs/cli-reference/json-output.md,
    kinder-site/src/content/docs/cli-reference/troubleshooting.md
  </files>
  <action>
**Comparison.astro** -- Add 2 new entries to BOTH columns so they stay in parity:

kind column (add after "No dashboard"):
- `No local registry`
- `No TLS certificates`

kinder column (add after "Headlamp -- web dashboard"):
- `Local Registry -- push images to localhost:5001`
- `cert-manager -- automatic TLS certificates`

Both columns should have 8 `<li>` items each (currently 6 each). Keep existing CSS unchanged.

**index.mdx** -- Two changes:

1. Update the frontmatter `description` field to mention all 7 addons. Change from:
```
description: kind with MetalLB, Envoy Gateway, Metrics Server, CoreDNS tuning, and Headlamp pre-installed.
```
to something like:
```
description: kind with MetalLB, Envoy Gateway, Metrics Server, CoreDNS tuning, Headlamp, Local Registry, and cert-manager pre-installed.
```

2. **Reorganize the Addons CardGrid into core vs optional groups** (per locked decision: "Group addons as core vs optional on landing page"). The current index.mdx has a single `## Addons` heading with one `<CardGrid>` containing all 7 cards. Replace this with two subsections:

```mdx
## Core Addons

Always-on essentials for every kinder cluster.

<CardGrid>
  <Card title="MetalLB" icon="seti:db">
    LoadBalancer services with real external IPs from your Docker or Podman subnet.

    [View docs](/addons/metallb/)
  </Card>
  <Card title="Metrics Server" icon="bars">
    Enables `kubectl top` and Horizontal Pod Autoscaler support.

    [View docs](/addons/metrics-server/)
  </Card>
  <Card title="CoreDNS Tuning" icon="setting">
    Autopath short names, verified pod records, and doubled cache TTL.

    [View docs](/addons/coredns/)
  </Card>
</CardGrid>

## Optional Addons

Powerful extras enabled by default -- disable with `--profile minimal` for lightweight clusters.

<CardGrid>
  <Card title="Envoy Gateway" icon="random">
    Gateway API routing with the `eg` GatewayClass pre-configured.

    [View docs](/addons/envoy-gateway/)
  </Card>
  <Card title="Headlamp Dashboard" icon="laptop">
    Web-based cluster UI. Access via port-forward, token printed at cluster creation.

    [View docs](/addons/headlamp/)
  </Card>
  <Card title="Local Registry" icon="seti:docker">
    Private container registry at `localhost:5001` with dev tool auto-discovery.

    [View docs](/addons/local-registry/)
  </Card>
  <Card title="cert-manager" icon="approve-check-circle">
    Automatic TLS certificates with a self-signed ClusterIssuer ready immediately.

    [View docs](/addons/cert-manager/)
  </Card>
</CardGrid>
```

Keep the existing hero, Install section, "kind vs kinder" Comparison section, and footer unchanged. Only the Addons section changes (replace the single `## Addons` + `<CardGrid>` with the two-section layout above). Order within each group follows importance: MetalLB first in core, Envoy Gateway first in optional.

**Placeholder files** -- Create directories `kinder-site/src/content/docs/guides/` and `kinder-site/src/content/docs/cli-reference/`. Create 6 minimal `.md` files with frontmatter + a single `:::note[Coming soon]` callout:

1. `guides/tls-web-app.md` -- title: "TLS Web App", description: "Deploy a TLS-secured web app with Local Registry, Envoy Gateway, cert-manager, and MetalLB."
2. `guides/hpa-auto-scaling.md` -- title: "HPA Auto-Scaling", description: "Set up Horizontal Pod Autoscaler with Metrics Server and watch pods scale under load."
3. `guides/local-dev-workflow.md` -- title: "Local Dev Workflow", description: "Code, build, push to localhost:5001, deploy, and iterate with the local registry."
4. `cli-reference/profile-comparison.md` -- title: "Profile Comparison", description: "Compare --profile presets: minimal, full, gateway, and ci."
5. `cli-reference/json-output.md` -- title: "JSON Output Reference", description: "Use --output json with kinder commands and filter results with jq."
6. `cli-reference/troubleshooting.md` -- title: "Troubleshooting", description: "Diagnose issues with kinder env and kinder doctor."

Each placeholder body is just:
```markdown
:::note[Coming soon]
This guide is in progress. Check back after the next release.
:::
```

**astro.config.mjs** -- Update the sidebar array to:
1. Add `collapsed: true` to the existing Addons group
2. Add a new "Guides" group with `collapsed: true` containing the 3 guide slugs
3. Add a new "CLI Reference" group with `collapsed: true` containing the 3 CLI reference slugs
4. Keep `{ slug: 'changelog' }` as the last entry

Final sidebar order: Installation, Quick Start, Configuration, Addons (collapsed), Guides (collapsed), CLI Reference (collapsed), Changelog.

Use `{ slug: 'guides/tls-web-app' }` format (no explicit label -- auto-derived from frontmatter title).
  </action>
  <verify>
    <automated>cd kinder-site && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Comparison.astro has 8 items per column including Local Registry and cert-manager. index.mdx description mentions all 7 addons. index.mdx has two separate addon sections: "Core Addons" (MetalLB, Metrics Server, CoreDNS) and "Optional Addons" (Envoy Gateway, Headlamp, Local Registry, cert-manager) each with their own CardGrid. astro.config.mjs sidebar has Addons (collapsed), Guides (collapsed, 3 items), CLI Reference (collapsed, 3 items). All 6 placeholder files exist with "Coming soon" note. Site builds without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update quick-start page with all 7 addon verifications, --profile tip, and doctor section</name>
  <files>kinder-site/src/content/docs/quick-start.md</files>
  <action>
Update `kinder-site/src/content/docs/quick-start.md` with these changes:

**1. Update cluster creation output** -- The example output block after `kinder create cluster` currently shows 5 addons. Update it to show all 7:
```
 * MetalLB              installed
 * Metrics Server       installed
 * CoreDNS Tuning       installed
 * Envoy Gateway        installed
 * Dashboard            installed
 * Local Registry       installed
 * cert-manager         installed
```
Also add the corresponding "Installing" progress lines:
```
 ✓ Installing MetalLB
 ✓ Installing Metrics Server
 ✓ Tuning CoreDNS
 ✓ Installing Envoy Gateway
 ✓ Installing Dashboard
 ✓ Installing Local Registry
 ✓ Installing cert-manager
```

**2. Add --profile tip callout** -- Place a `:::tip[Addon profiles]` callout AFTER the creation output block but BEFORE the "What You Get" section. Content:
```
:::tip[Addon profiles]
`kinder create cluster` enables all 7 addons by default. Use `--profile` to select a preset:

- `--profile minimal` — no kinder addons (plain kind cluster)
- `--profile gateway` — MetalLB + Envoy Gateway only
- `--profile ci` — Metrics Server + cert-manager (CI-optimized)
- `--profile full` — all addons (same as default)
:::
```

**3. Update "What You Get" table** -- Add 2 rows:
| Local Registry | `default` (host network) | Private container registry at localhost:5001 |
| cert-manager | `cert-manager` | Automatic TLS certificates with self-signed ClusterIssuer |

**4. Restructure "Verify Addons" into core/optional grouping** -- Add subheadings:
- `## Verify Core Addons` with MetalLB, Metrics Server, CoreDNS Tuning sections (already exist)
- `## Verify Optional Addons` with Envoy Gateway, Headlamp Dashboard (already exist), plus 2 new sections:

**5. Add Local Registry verification section** (under Optional Addons):
```markdown
### Local Registry

Confirm the registry container is running and accessible:

```sh
docker ps --filter name=kind-registry
```

Expected output:

```
CONTAINER ID   IMAGE        COMMAND                  PORTS                    NAMES
abc123def456   registry:2   "/entrypoint.sh /etc…"   0.0.0.0:5001->5000/tcp   kind-registry
```

Verify dev tool discovery ConfigMap is present:

```sh
kubectl get configmap local-registry-hosting -n kube-public
```

Expected output:

```
NAME                     DATA   AGE
local-registry-hosting   1      60s
```
```

**6. Add cert-manager verification section** (under Optional Addons):
```markdown
### cert-manager

Check all three cert-manager components are running:

```sh
kubectl get pods -n cert-manager
```

Expected output:

```
NAME                                       READY   STATUS    RESTARTS   AGE
cert-manager-...                           1/1     Running   0          60s
cert-manager-cainjector-...                1/1     Running   0          60s
cert-manager-webhook-...                   1/1     Running   0          60s
```

Confirm the self-signed ClusterIssuer is ready:

```sh
kubectl get clusterissuer selfsigned-issuer
```

Expected output:

```
NAME                READY   AGE
selfsigned-issuer   True    60s
```
```

**7. Add "Something wrong?" section** -- At the end, before "Delete the Cluster":
```markdown
## Something wrong?

Run `kinder doctor` to check prerequisites and identify issues:

```sh
kinder doctor
```

This checks that Docker (or Podman/nerdctl), kubectl, and other dependencies are installed and reachable.
```

Keep the existing note about the cluster name tip at the top. Keep the existing "Delete the Cluster" section at the very end.
  </action>
  <verify>
    <automated>cd kinder-site && npm run build 2>&1 | tail -5 && grep -c "Local Registry\|cert-manager\|--profile\|kinder doctor" src/content/docs/quick-start.md</automated>
  </verify>
  <done>
    Quick-start page shows all 7 addons in creation output. Has verification sections for all 7 addons grouped as core (MetalLB, Metrics Server, CoreDNS) and optional (Envoy Gateway, Headlamp, Local Registry, cert-manager). Contains --profile tip callout after creation output. Contains "Something wrong?" section with kinder doctor. Site builds without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update configuration page with all 7 addon fields and core/optional grouping</name>
  <files>kinder-site/src/content/docs/configuration.md</files>
  <action>
Update `kinder-site/src/content/docs/configuration.md` with these changes:

**1. Add a full v1alpha4 config example at the top** -- After the existing "Cluster Configuration" section and before "Using a Config File", add a new section:

```markdown
## Complete Configuration Example

A complete kinder configuration showing all addon fields:

```yaml
# Full kinder v1alpha4 configuration -- all fields shown
apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
addons:
  # Core addons (always useful -- enabled by default)
  metalLB: true
  metricsServer: true
  coreDNSTuning: true
  # Optional addons (enabled by default -- disable for lightweight clusters)
  envoyGateway: true
  dashboard: true
  localRegistry: true
  certManager: true
```

**2. Add a prominent callout** -- Right after the full config example:

```markdown
:::note[All addons enabled by default]
All 7 addons are installed by default. The **core** group (MetalLB, Metrics Server, CoreDNS) is always useful for any workflow. The **optional** group (Envoy Gateway, Headlamp, Local Registry, cert-manager) is also enabled by default but commonly disabled for lightweight or CI clusters using `--profile minimal` or `--profile ci`.
:::
```

**3. Restructure the "Addon Fields" section** -- Replace the current single table with two grouped subsections:

```markdown
## Addon Fields

### Core Addons

These addons are always useful regardless of your workload.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `metalLB` | `bool` | `true` | Install [MetalLB](https://metallb.universe.tf/) for LoadBalancer IP assignment |
| `metricsServer` | `bool` | `true` | Install [Metrics Server](https://github.com/kubernetes-sigs/metrics-server) for `kubectl top` support |
| `coreDNSTuning` | `bool` | `true` | Apply CoreDNS tuning for optimised local DNS caching |

### Optional Addons

These addons are powerful but commonly disabled for minimal or CI clusters.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `envoyGateway` | `bool` | `true` | Install [Envoy Gateway](https://gateway.envoyproxy.io/) for Gateway API ingress |
| `dashboard` | `bool` | `true` | Install [Headlamp](https://headlamp.dev/) web dashboard |
| `localRegistry` | `bool` | `true` | Run a private container registry at `localhost:5001` with dev tool auto-discovery |
| `certManager` | `bool` | `true` | Install [cert-manager](https://cert-manager.io/) with a self-signed ClusterIssuer |
```

**4. Update "Disabling Addons" examples** -- Update the "skip all addons" YAML example to include all 7 fields:

```yaml
apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
addons:
  metalLB: false
  envoyGateway: false
  metricsServer: false
  coreDNSTuning: false
  dashboard: false
  localRegistry: false
  certManager: false
```

Keep the existing partial disable example but update the description to reflect all 7 addons being available. Keep the "Compatibility with kind" section at the bottom unchanged.
  </action>
  <verify>
    <automated>cd kinder-site && npm run build 2>&1 | tail -5 && grep -c "localRegistry\|certManager" src/content/docs/configuration.md</automated>
  </verify>
  <done>
    Configuration page has full v1alpha4 YAML example at top with core/optional comments. Has callout explaining all 7 addons enabled by default with core vs optional framing. Addon fields section split into Core Addons table (metalLB, metricsServer, coreDNSTuning) and Optional Addons table (envoyGateway, dashboard, localRegistry, certManager). "Skip all addons" example includes all 7 fields. Site builds without errors.
  </done>
</task>

</tasks>

<verification>
1. `cd kinder-site && npm run build` completes with zero errors
2. `grep -c "Local Registry" kinder-site/src/components/Comparison.astro` returns at least 1
3. `grep -c "cert-manager" kinder-site/src/components/Comparison.astro` returns at least 1
4. `grep "Local Registry\|cert-manager" kinder-site/src/content/docs/index.mdx` shows both in description
5. `grep "Core Addons" kinder-site/src/content/docs/index.mdx` returns a match (card grouping)
6. `grep "Optional Addons" kinder-site/src/content/docs/index.mdx` returns a match (card grouping)
7. `grep -c "localRegistry\|certManager" kinder-site/src/content/docs/configuration.md` returns at least 4
8. `grep -- "--profile" kinder-site/src/content/docs/quick-start.md` returns matches
9. `grep "kinder doctor" kinder-site/src/content/docs/quick-start.md` returns matches
10. `grep "Guides\|CLI Reference" kinder-site/astro.config.mjs` returns matches
11. All 6 placeholder files exist and contain "Coming soon"
</verification>

<success_criteria>
- Landing page Comparison component lists all 7 addons with matching kind column entries
- Landing page description meta mentions all 7 addons
- Landing page addon cards grouped into "Core Addons" (MetalLB, Metrics Server, CoreDNS) and "Optional Addons" (Envoy Gateway, Headlamp, Local Registry, cert-manager) with separate CardGrid components
- Quick-start page verifies all 7 addons with commands and expected output, grouped core/optional
- Quick-start page has --profile tip callout after main create flow
- Quick-start page has "Something wrong?" section with kinder doctor
- Configuration page has full YAML example at top, core/optional grouped field tables, all 7 fields documented
- Sidebar has Addons (collapsed), Guides (collapsed, 3 items), CLI Reference (collapsed, 3 items)
- All 6 placeholder files exist with "Coming soon" note
- `npm run build` in kinder-site/ succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-foundation-fixes/30-01-SUMMARY.md`
</output>
