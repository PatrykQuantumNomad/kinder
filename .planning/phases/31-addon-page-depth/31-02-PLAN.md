---
phase: 31-addon-page-depth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - kinder-site/src/content/docs/addons/headlamp.md
  - kinder-site/src/content/docs/addons/local-registry.md
  - kinder-site/src/content/docs/addons/cert-manager.md
autonomous: true
requirements: [ADDON-05, ADDON-06, ADDON-07]

must_haves:
  truths:
    - "Headlamp page describes main dashboard areas users can navigate"
    - "Headlamp page has a troubleshooting section covering invalid token error"
    - "Local Registry page shows a multi-image build-and-push workflow"
    - "Local Registry page shows how to clean up or overwrite images"
    - "Local Registry page has a troubleshooting section covering ImagePullBackOff"
    - "cert-manager page has an additional certificate example beyond the existing single-host cert"
    - "cert-manager page has a troubleshooting section covering READY: False"
  artifacts:
    - path: "kinder-site/src/content/docs/addons/headlamp.md"
      provides: "Headlamp navigation guide and troubleshooting"
      contains: "Troubleshooting"
    - path: "kinder-site/src/content/docs/addons/local-registry.md"
      provides: "Local Registry multi-image workflow, cleanup, and troubleshooting"
      contains: "v2/_catalog"
    - path: "kinder-site/src/content/docs/addons/cert-manager.md"
      provides: "cert-manager wildcard cert example and troubleshooting"
      contains: "wildcard"
  key_links:
    - from: "kinder-site/src/content/docs/addons/local-registry.md"
      to: "localhost:5001"
      via: "curl v2/_catalog"
      pattern: "curl.*localhost:5001/v2/_catalog"
    - from: "kinder-site/src/content/docs/addons/cert-manager.md"
      to: "selfsigned-issuer"
      via: "issuerRef in Certificate YAML"
      pattern: "selfsigned-issuer"
---

<objective>
Enrich Headlamp, Local Registry, and cert-manager addon pages with practical examples and troubleshooting sections.

Purpose: These three addon pages currently lack navigation guidance (Headlamp), multi-image workflows (Registry), and certificate variety (cert-manager). Users need concrete usage patterns and troubleshooting to be productive.
Output: Three enriched addon Markdown files with practical examples and troubleshooting sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-addon-page-depth/31-RESEARCH.md
@kinder-site/src/content/docs/addons/headlamp.md
@kinder-site/src/content/docs/addons/local-registry.md
@kinder-site/src/content/docs/addons/cert-manager.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich Headlamp and Local Registry addon pages</name>
  <files>kinder-site/src/content/docs/addons/headlamp.md, kinder-site/src/content/docs/addons/local-registry.md</files>
  <action>
**headlamp.md — Add two new sections at the end of the file:**

1. Add a `## What you can do` section after "How to disable" describing the main areas of the Headlamp dashboard. Use a brief intro sentence ("Once logged in, Headlamp provides a full cluster overview. Here are the main areas you will use:") followed by 4-5 items:

   - **Workloads** — View Deployments, Pods, ReplicaSets, DaemonSets, Jobs. Click any pod to see its status, containers, and resource usage.
   - **Logs** — Click a pod, then select a container to stream its logs in real time. Use the search bar to filter log lines.
   - **Events** — The cluster Events view shows recent scheduling decisions, image pulls, and errors across all namespaces. Useful for debugging pod startup failures.
   - **Config** — Browse ConfigMaps and Secrets. kinder's `kinder-dashboard-token` secret appears here under `kube-system`.
   - **Nodes** — View node status, capacity, and allocated resources. In a single-node kinder cluster, this shows the control-plane node.

2. Add a `## Troubleshooting` section after "What you can do" with one entry:

   `### Invalid token error` — Symptom: Headlamp shows "Invalid token" or "Unauthorized" after pasting the token. Cause: the raw base64-encoded value was pasted instead of the decoded token. The `kubectl get secret` command returns base64-encoded data; the `| base64 --decode` step is required. Fix: re-run the token retrieval command ensuring the `| base64 --decode` pipe is included:
   ```sh
   kubectl get secret kinder-dashboard-token -n kube-system \
     -o jsonpath='{.data.token}' | base64 --decode
   ```
   Copy the decoded output (a long JWT string) and paste it into the token field.

**local-registry.md — Add three new sections before "Platform notes":**

1. Add a `## Multi-image workflow` section after "Dev tool integration" showing how to build and push multiple images and verify they are all in the registry:

   Show building and pushing two images (`frontend:v1` and `backend:v1`) to `localhost:5001`, then verify with `curl http://localhost:5001/v2/_catalog` and expected JSON output `{"repositories":["backend","frontend"]}`. Add a deployment YAML snippet showing both images referenced from `localhost:5001/`.

2. Add a `## Cleaning up images` section after "Multi-image workflow":

   Explain the simplest cleanup approach: re-push with the same tag to overwrite. For listing tags of a specific image: `curl http://localhost:5001/v2/myapp/tags/list`. Note that `registry:2` does not garbage-collect by default — disk space is reclaimed only when the registry container is recreated. For a fresh start: `docker rm -f kind-registry` (it will be recreated on next `kinder create cluster`).

3. Add a `## Troubleshooting` section after "Cleaning up images" (before "Platform notes") with one entry:

   `### ImagePullBackOff from localhost:5001` — Symptom: `kubectl describe pod` shows `Failed to pull image "localhost:5001/myapp:latest"` with `ImagePullBackOff`. Cause: the `kind-registry` container is not running — it may have been stopped or removed. Fix: check with `docker ps --filter name=kind-registry`. If not running, start it with `docker start kind-registry`. If it was removed, the simplest fix is to recreate the cluster with `kinder create cluster` which will recreate the registry.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder/kinder-site && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - headlamp.md contains "What you can do" section listing 4-5 dashboard areas
    - headlamp.md contains "Troubleshooting" section with invalid token entry
    - local-registry.md contains "Multi-image workflow" section with curl v2/_catalog verification
    - local-registry.md contains "Cleaning up images" section
    - local-registry.md contains "Troubleshooting" section with ImagePullBackOff entry
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich cert-manager addon page</name>
  <files>kinder-site/src/content/docs/addons/cert-manager.md</files>
  <action>
**cert-manager.md — Add two new sections before "Technical notes":**

1. Add a `## More certificate examples` section after "How to disable" with one subsection:

   `### Wildcard certificate with custom duration` — Show a Certificate YAML that issues a wildcard cert for `*.example.local` with multiple SANs (`*.example.local` and `example.local`), custom `duration: 2160h` (90 days), and `renewBefore: 360h` (15 days before expiry). The cert uses `issuerRef` pointing to `selfsigned-issuer` with `kind: ClusterIssuer`. Show `kubectl apply` and `kubectl get certificate` to verify it reaches `READY: True`. Add a `:::tip` callout explaining when to use wildcards (multiple subdomains under the same domain) vs individual certs (single service).

   Emphasize: always use `kind: ClusterIssuer` (not `kind: Issuer`) when referencing `selfsigned-issuer` because kinder creates it as a ClusterIssuer, not a namespace-scoped Issuer.

2. Add a `## Troubleshooting` section after "More certificate examples" (before "Technical notes") with two entries:

   `### Certificate stays READY: False` — Symptom: `kubectl get certificate` shows `READY: False` and it does not change. Two common causes:

   **(a) Webhook not ready yet:** cert-manager's webhook takes 30-60 seconds to become ready after cluster creation. If you apply a Certificate immediately after `kinder create cluster`, it may fail validation. Fix: wait 60 seconds and reapply, or check webhook readiness with `kubectl get pods -n cert-manager`.

   **(b) Wrong issuer kind:** The Certificate spec has `kind: Issuer` but `selfsigned-issuer` is a `ClusterIssuer`. Fix: change `kind: Issuer` to `kind: ClusterIssuer` in the `issuerRef` block.

   Show diagnostic commands: `kubectl describe certificate <name>` to see events, and `kubectl get certificaterequest` to check if the request was created.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/kinder/kinder-site && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - cert-manager.md contains "More certificate examples" section with wildcard cert YAML
    - cert-manager.md wildcard example uses kind: ClusterIssuer (not Issuer)
    - cert-manager.md wildcard example includes duration and renewBefore fields
    - cert-manager.md contains "Troubleshooting" section with READY: False entry covering both webhook timing and Issuer vs ClusterIssuer
    - cert-manager.md troubleshooting includes kubectl describe certificate diagnostic command
  </done>
</task>

</tasks>

<verification>
1. All three addon pages build without errors: `cd kinder-site && npm run build`
2. Headlamp page has a dashboard navigation guide section
3. Headlamp page has a troubleshooting section with the invalid token entry
4. Local Registry page has multi-image workflow and cleanup sections
5. Local Registry page has a troubleshooting section with ImagePullBackOff entry
6. cert-manager page has an additional certificate example (wildcard)
7. cert-manager page has a troubleshooting section covering READY: False with two causes
8. No new files created — only the three existing .md files modified
</verification>

<success_criteria>
- ADDON-05: Headlamp page has navigation guide and troubleshooting
- ADDON-06: Local Registry page has multi-image workflow, cleanup, and troubleshooting
- ADDON-07: cert-manager page has wildcard cert example and troubleshooting
- All three pages pass `npx astro check` with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-addon-page-depth/31-02-SUMMARY.md`
</output>
