# Requirements Archive: v2.3 Inner Loop

**Archived:** 2026-05-07
**Status:** SHIPPED (with one external-blocker deferral)

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: Kinder v2.3 Inner Loop

**Defined:** 2026-05-03
**Core Value:** A single command gives developers a local Kubernetes cluster where LoadBalancer services, Gateway API routing, metrics, and dashboards all work without any manual setup.
**Milestone Goal:** Make daily iteration on a kinder cluster as fast as creating one — pause/resume to reclaim laptop resources, snapshot/restore for instant clean state, hot-reload for code changes, and runtime error decoding extending the v2.1 doctor framework. Includes a sync phase for kind upstream's HAProxy→Envoy LB transition and K8s 1.36 default.

## v2.3 Requirements

Requirements for the Inner Loop milestone. Each maps to exactly one phase.

### LIFE — Cluster Lifecycle

- [x] **LIFE-01**: User can pause a running cluster via `kinder pause [name]`, freeing CPU/RAM without losing state
- [x] **LIFE-02**: User can resume a paused cluster via `kinder resume [name]`; pods, PVs, and node state are preserved
- [x] **LIFE-03**: Pause-resume orchestrates control-plane and worker stop/start in correct order to preserve etcd quorum on HA clusters
- [x] **LIFE-04**: Doctor pre-flight check `cluster-resume-readiness` runs before resume on HA clusters and warns if etcd quorum is at risk
- [x] **LIFE-05**: User can snapshot a cluster via `kinder snapshot create [snap-name]`; snapshot captures etcd state, loaded container images, and local-path-provisioner PV contents
- [x] **LIFE-06**: User can restore a cluster from a snapshot via `kinder snapshot restore [snap-name]`; restore refuses if Kubernetes version in snapshot mismatches current cluster
- [x] **LIFE-07**: User can list, inspect (size, age, k8s version), and prune snapshots via `kinder snapshot list`, `kinder snapshot show [snap-name]`, `kinder snapshot prune`
- [x] **LIFE-08**: Snapshot metadata records cluster Kubernetes version, addon versions, and image-bundle digest for air-gap reproducibility

### DEV — Inner Loop

- [x] **DEV-01**: User can run `kinder dev --watch <dir> --target <deployment>` to watch a directory and trigger a build-load-rollout pipeline on file changes
- [x] **DEV-02**: `kinder dev` builds an image from the watched directory (Dockerfile-based) and imports it via the existing `kinder load images` pipeline
- [x] **DEV-03**: After successful image load, `kinder dev` rolls the target Deployment via `kubectl rollout restart` and waits for ready
- [x] **DEV-04**: `kinder dev` debounces rapid file changes (configurable, default 500ms) and shows build/load/rollout timing per cycle
- [x] **DEV-05**: `kinder dev` supports `--poll` flag for fsnotify-unfriendly environments (Docker Desktop volume mounts on macOS); polls watched directory at configurable interval

### DIAG — Runtime Diagnostics (extends v2.1 doctor framework)

- [x] **DIAG-01**: User can run `kinder doctor decode` to scan recent docker logs and `kubectl get events` for known error patterns and print plain-English explanations with suggested fixes
- [x] **DIAG-02**: Decoder ships with at least 15 cataloged error patterns covering kubelet, kubeadm, containerd, docker, and addon-startup failures (delivered: 16 patterns)
- [x] **DIAG-03**: Each decoded error includes: pattern matched, plain-English explanation, suggested fix, link to docs/issue (where applicable)
- [x] **DIAG-04**: `kinder doctor decode --auto-fix` applies known-safe remediations automatically (whitelist only — never destructive)

### SYNC — Upstream Sync & K8s 1.36

- [x] **SYNC-01**: Adopt kind PR #4127 — replace HAProxy load-balancer with Envoy in HA cluster setups; drop `kindest/haproxy` image dependency
- [ ] **SYNC-02** [DEFERRED]: Default `kindest/node` image bumped to a Kubernetes 1.36.x patch release (latest stable at ship time, ≥1.36.4 to avoid kind #4131 regression). **External blocker**: `kindest/node:v1.36.x` not yet published on Docker Hub as of 2026-05-07. Plan 51-04 halted cleanly at gating probe; Task 2 authored and ready to execute atomically once kind v0.32.0 publishes the image.
- [x] **SYNC-03**: Cluster config validation rejects `kubeProxyMode: ipvs` on K8s 1.36+ with a clear error message pointing to the iptables migration path
- [x] **SYNC-04**: Website ships a "What's new in K8s 1.36" recipe page demonstrating User Namespaces (GA) and In-Place Pod Resize (GA) on a kinder cluster

## v2.4+ Requirements (Carried Forward)

### Carried from v2.3

- **SYNC-02** (deferred): Default node image bump to K8s 1.36.x — re-run plan 51-04 once `kindest/node:v1.36.x` is published

### Tech Debt (deferred from CONCERNS audit)

- **DEBT-01**: Provider de-duplication — extract docker/podman/nerdctl shared code parameterized by binary
- **DEBT-02**: context.Context cancellation through `cluster.Create()` and node-provisioning blocking ops
- **DEBT-03**: kubeadm v1beta4 generator alongside v1beta3 (pick by node K8s version)
- **DEBT-04** (new in v2.3): Pre-existing data race in `pkg/internal/doctor/check_test.go` and `socket_test.go` (`allChecks` global mutated under `t.Parallel()`) — confirmed pre-50 baseline at commit `c138ad62`

### Addon Bumps

- **ADDON-01**: cert-manager v1.16.3 → v1.20.x (UID/GID 1000→65532 audit required)
- **ADDON-02**: Envoy Gateway v1.3.1 → v1.7.x (staged 1.3 → 1.5 → 1.7)
- **ADDON-03**: Headlamp v0.40.1 → v0.41.x (MCP server for AI assistants)

### Lifecycle Expansion

- **LIFE-09**: Pause/resume support for podman provider
- **LIFE-10**: Pause/resume support for nerdctl provider
- **LIFE-11**: Snapshot remote-storage backend (S3/GCS) for team sharing

## Out of Scope

Explicitly excluded from kinder. Documented to prevent scope creep.

| Feature | Reason |
|---|---|
| Cilium-by-default CNI swap | Kernel/Docker Desktop pain; blast radius too large for default; opt-in addon path remains available |
| Tiltfile-style DSL for `kinder dev` | Compete-with-Tilt loses; stay imperative-CLI minimal |
| Velero bundle for backup/restore | Overkill for laptops; lightweight `kinder snapshot` covers actual dev pain |
| Web UI for cluster management | Headlamp covers it (already shipped); UX treadmill |
| Apple Containerization runtime support | Surface unstable; revisit in 6-12 months |
| `kinder fleet` multi-cluster command | Different milestone theme; vind/k3d coverage exists |
| `kinder observe` LGTM stack preset | Different milestone theme; resource-heavy; opt-in path via addons remains |
| `kinder gpu ollama` AI runtime preset | Different milestone theme; revisit if user demand surfaces |
| `kinder gitops` Argo/Flux preset | Niche; defer until requested |
| `kinder runner` GitHub Actions runner preset | Niche; defer until requested |

## Traceability

Final mapping of v2.3 requirements to phases.

| Requirement | Phase | Status |
|-------------|-------|--------|
| LIFE-01 | 47 | Complete (47-02, 47-06) |
| LIFE-02 | 47 | Complete (47-03, 47-06) |
| LIFE-03 | 47 | Complete (47-02 pause-side + 47-03 resume-side) |
| LIFE-04 | 47 | Complete (47-04, 47-05, 47-06) |
| LIFE-05 | 48 | Complete |
| LIFE-06 | 48 | Complete |
| LIFE-07 | 48 | Complete (48-05) |
| LIFE-08 | 48 | Complete |
| DEV-01 | 49 | Complete |
| DEV-02 | 49 | Complete |
| DEV-03 | 49 | Complete |
| DEV-04 | 49 | Complete |
| DEV-05 | 49 | Complete |
| DIAG-01 | 50 | Complete |
| DIAG-02 | 50 | Complete (16 patterns vs 15 required) |
| DIAG-03 | 50 | Complete |
| DIAG-04 | 50 | Complete |
| SYNC-01 | 51 | Complete (51-01) |
| **SYNC-02** | **51** | **DEFERRED — external blocker (kindest/node:v1.36.x not on Docker Hub)** |
| SYNC-03 | 51 | Complete (51-02) |
| SYNC-04 | 51 | Complete (51-03) |

**Coverage:**
- v2.3 requirements: 21 total
- Mapped to phases: 21
- Unmapped: 0
- Shipped: 20
- Deferred (external): 1

---

## Milestone Summary

**Shipped:** 20 of 21 requirements

**Adjusted:** None — all requirements delivered as specified.

**Dropped:** None.

**Deferred:** SYNC-02 (default node image bump to K8s 1.36.x) — external upstream blocker. Plan 51-04 halted cleanly at the gating probe per its INCONCLUSIVE protocol. Task 2 (TDD RED→GREEN for `pkg/apis/config/defaults/image.go`) is fully authored and ready to execute atomically once `kindest/node:v1.36.x` is published on Docker Hub. No source-level rework required. Carried forward for re-execution in v2.4 (or as a same-milestone amendment when the image lands).

**Live UAT Status:**
- Phase 48: Live UAT approved 2026-05-06 (`make integration` + manual smoke on `uat48` cluster)
- Phase 49: Live UAT approved 2026-05-06 (real cluster hot-reload cycle 4.1s on `openshell-dev`)
- Phase 50: Live UAT approved 2026-05-07 (5-step UAT on `openshell-dev`, K8s 1.35.0)
- Phase 47: source-complete; awaits developer rebuild + live HA smoke (documented carry-forward)
- Phase 51: source-complete; awaits live HA Envoy LB smoke + IPVS-config CLI rejection smoke (documented carry-forward)

---
*Archived: 2026-05-07 as part of v2.3 milestone completion*
