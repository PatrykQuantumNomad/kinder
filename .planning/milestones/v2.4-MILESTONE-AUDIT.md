---
milestone: v2.4
milestone_name: Hardening
audited: 2026-05-31T00:00:00Z
status: passed
scores:
  requirements: 13/13 satisfied + 1 correctly deferred (SYNC-05 external blocker)
  phases: 11/11 verified
  integration: 5/5 cross-phase wiring chains confirmed
  flows: 5/5 E2E flows traceable end-to-end
gaps: []
tech_debt:
  - phase: 57 (DIAG-06 SC2 cosmetic wording)
    items:
      - "Doctor cluster-resume-readiness emits `etcd endpoint health probe failed` rather than SC2-specified `quorum at risk`. Behavior correct, wording diverges. Filed as v2.5 follow-up (commits 70ee57ac + 0bc81d77)."
  - phase: doctor (latent fragility)
    items:
      - "`nerdctl.lima` basename fragility in IPAM probe `VerdictUnsupported` short-circuit (`binary == \"nerdctl\"` does NOT match `nerdctl.lima`). Masked today by `network disconnect`/`network connect` error path. Low severity; track for future doctor hardening."
  - phase: CI / UAT plumbing
    items:
      - "UAT scripts (`hack/uat-47-ha-smoke.sh`, `hack/uat-51-envoy-ipvs-guide.sh`, `hack/uat-57.3-cert-regen.sh`, `hack/uat-57.4-ipam-probe.sh`) are manual-only — no CI wiring (self-hosted runner / OS matrix). Deferred to v2.5."
      - "`./bin/kinder version` does not print git hash (versionPreRelease suppresses it). Strings-marker gate used as workaround in Phase 58. Track for v2.5 cosmetic phase."
  - phase: 55 (CI policy)
    items:
      - "`build-check.yml` (Windows cross-compile) is not yet a required status check on `main` branch protection. Workflow runs on every PR but does not block merge. Repo-admin task; not a code gap."
      - "`macos-sign-verify.yml` triggers on `push: branches: [main]` (post-merge drift detection), not on PRs. Matches design intent; flagged for awareness."
deferred:
  - requirement: SYNC-05
    addressed_in: "Phase 53-00 re-run when kind publishes v0.32.0 + kindest/node:v1.36.x to Docker Hub"
    evidence: "Plan 53-00 Outcome B halting condition satisfied; `pkg/apis/config/defaults/image.go` correctly remains at v1.35.1; re-run instructions in 53-00-SUMMARY.md"
---

# v2.4 Hardening — Milestone Audit

**Audited:** 2026-05-31
**Status:** `passed` — All 13 active requirements SATISFIED; 1 requirement correctly DEFERRED (SYNC-05, external blocker). Cross-phase integration verified by independent checker (5/5 chains, 5/5 flows). All 11 phases have VERIFICATION.md artifacts. No critical blockers. Tech debt is non-blocking and scoped for v2.5.

---

## Requirements Coverage

| Req | Phase | Description | Code Evidence | REQUIREMENTS.md Status | Verdict |
|-----|-------|-------------|---------------|------------------------|---------|
| **LIFE-09** | 52 (+57.1, 57.2, 57.3, 57.4) | HA pause/resume preserves etcd peer connectivity | IP-pin via `docker network connect --ip`; cert-regen fallback widened to 4 cert types; `--strategy` flag; `--privileged` IPAM probe fix | `[x]` Complete | **SATISFIED** |
| **ADDON-01** | 53-01 | local-path-provisioner v0.0.36 (CVE security fix) | `localpathprovisioner.go` Images pinned; manifest line 91 matches; `TestImagesPinsV0036` PASS | `[x]` Complete | **SATISFIED** |
| **ADDON-02** | 53-02 | Headlamp v0.42.0 with token-print verified | `dashboard.go` Images pinned; UAT-2 Path A confirmed kubectl auth + UI 200 | `[x]` Complete | **SATISFIED** |
| **ADDON-03** | 53-03 | cert-manager v1.20.2 with `--server-side` | `certmanager.go` 3 images v1.20.2; Execute line 79 includes `--server-side`; UAT-3 ClusterIssuer issued | `[x]` Complete | **SATISFIED** |
| **ADDON-04** | 53-04 | Envoy Gateway v1.7.2 with HTTPRoute UAT | `envoygw.go` v1.7.2; certgen job name verified; bundle v1.4.1 verified; UAT-4 HTTP 200 | `[x]` Complete | **SATISFIED** |
| **ADDON-05** | 53-07 | `allAddonImages` reflects all bumped/held tags; count=14 | `offlinereadiness.go` allAddonImages = 14 entries; `TestAllAddonImages_*` PASS | `[x]` Complete | **SATISFIED** |
| **SYNC-05** | 53-00 | Default `kindest/node` bump to K8s 1.36.x (conditional) | Outcome B halting condition satisfied; `image.go` correctly unchanged at v1.35.1 | `[ ]` DEFERRED | **DEFERRED (external blocker — correct)** |
| **DIST-01** | 54 | macOS GoReleaser artifacts ad-hoc signed | `.goreleaser.yaml:44-46` darwin-gated codesign post-hook; CI run `25746519788` green; both arches `satisfies its Designated Requirement` | `[x]` Complete | **SATISFIED** |
| **DIST-02** | 55 | Windows PR-CI cross-compile blocking | `.github/workflows/build-check.yml` runs windows/amd64; CI run `25750801764` green; no `continue-on-error` | `[x]` Complete | **SATISFIED** |
| **DEBT-04** | 56 | `allChecks` race eliminated under `t.Parallel()` | `runChecks(checks []Check)` helper at `check.go:133`; race gate `make test-race-doctor` green @ count=100 | `[x]` Complete | **SATISFIED** |
| **DIAG-05** | 57 | `cluster-node-skew` skips external-LB/external-etcd roles | Inline guard at `clusterskew.go:118-126` references `constants.ExternalLoadBalancerNodeRoleValue` + `ExternalEtcdNodeRoleValue` | `[x]` Complete | **SATISFIED** |
| **DIAG-06** | 57 | `cluster-resume-readiness` parses etcdctl JSON for actionable text | Tolerant flow at `resumereadiness.go:173-203`; `parseEtcdHealth` called BEFORE verdict; raw-error dump removed | `[x]` Complete | **SATISFIED** (wording divergence noted as v2.5 follow-up) |
| **UAT-01** | 58-01 | Phase 47 live HA UAT closed | `hack/uat-47-ha-smoke.log` (HEAD `e0ec855e`, 8/8 PASS); 47-UAT.md flipped to closed (14/14 pass) | `[x]` Complete | **SATISFIED** |
| **UAT-02** | 58-02 | Phase 51 live UAT closed | `hack/uat-51-envoy-ipvs-guide.log` (HEAD `6b8c4f74`, 3/3 PASS); 51-UAT.md augmented (Option A) | `[x]` Complete | **SATISFIED** |

**Coverage:** 13/13 active SATISFIED + 1 correctly DEFERRED = **14/14 substantively addressed**. **No unsatisfied requirements.**

---

## Phase Verification Status

| Phase | Status | Score | VERIFICATION.md | Notes |
|-------|--------|-------|-----------------|-------|
| 52 — HA Etcd Peer-TLS Fix | passed | 2/4 static + 2 human (closed via Phase 58) | ✓ | Original gap (doctor race) addressed by Phase 56; live UAT items closed by Phase 58 |
| 53 — Addon Audit + Bumps + SYNC-05 | verified | 6/6 (SC1+SC3 gap-closed via 53-08) | ✓ | All overrides resolved by ROADMAP SC wording revision |
| 54 — macOS Ad-Hoc Code Signing | passed | 4/4 | ✓ | CI re-verified at `25746519788` |
| 55 — Windows PR-CI Build Step | passed | 3/3 | ✓ | CI green `25750801764` |
| 56 — DEBT-04 Doctor Test Race Fix | passed | 3/3 | ✓ | `-race -count=100` green @ 2.662s |
| 57 — Doctor Cosmetic Fixes | passed | 3/3 | ✓ | DIAG-06 wording divergence filed as v2.5 follow-up |
| 57.1 — LB cds/lds reapply (INSERTED) | passed | 3/3 code; 3 SCs deferred → closed via 58 | ✓ | Live-UAT items closed by Phase 58 |
| 57.2 — `discoverLBIPv6` rewrite (INSERTED) | passed | 6/6 | ✓ | Live IPv4 + IPv6 cluster UAT both PASS |
| 57.3 — HA cert-regen recovery (INSERTED) | passed | 4/4 code; SC5 deferred → closed via 58 | ✓ | 3 fixtures (IPv4 230s, IPv6 502s, dual 506s) |
| 57.4 — IPAM probe regression (INSERTED) | passed | 4/4 (macOS DD UAT 3/3 + retroactive code verify) | ✓ (written 2026-05-31) | `--privileged` fix at `ipamprobe.go:111` verified |
| 58 — Live UAT Closure for Phase 47+51 | passed | 4/4 | ✓ | UAT-01 + UAT-02 attested via 2 committed log files |

**Verified phases: 11/11.** All artifacts present.

---

## Cross-Phase Integration

Independent integration check (gsd-integration-checker) confirmed **5/5 wiring chains** and **5/5 end-to-end flows**. Two advisory warnings noted (both repo-admin, non-blocking).

### Wiring chains verified

1. **HA Resume strategy dispatch (52 → 57.1 → 57.2 → 57.3 → 57.4)** — `resume.go:267-283` strategy switch reads `opts.Strategy`; `reapplyLBConfig` called Phase 1.25 at `resume.go:348-359`; `loadbalancer.ClusterIPFamily` helper at `lbconfig.go:135` replaces removed `discoverLBIPv6`; `--privileged` IPAM probe at `ipamprobe.go:111`; `--strategy` cobra flag at `resume.go:88-89`. **WIRED.**
2. **Addon bumps propagated (53)** — `offlinereadiness.go` `allAddonImages` lists local-path v0.0.36 (line 71), Headlamp v0.42.0 (line 67), cert-manager v1.20.2 (lines 60-62, ×3), Envoy Gateway v1.7.2 (line 65); 14 total entries; `TestAllAddonImages_CountMatchesExpected` enforces count at `offlinereadiness_test.go:122-125`. **WIRED.**
3. **Doctor race-clean + cosmetics (56 → 57)** — `runChecks([]Check)` helper at `check.go:133`; `RunAllChecks` is 1-line delegate at `check.go:122-124`; `clusterskew.go:118-126` skips external-LB/external-etcd; `resumereadiness.go:180+213+223+243` emits actionable member-count text. **WIRED.**
4. **CI gates (54 → 55 → 56)** — `.github/workflows/build-check.yml` (Windows cross-compile), `race-check.yml` (`-race -count=100`), `macos-sign-verify.yml` (codesign verify) all exist; `.goreleaser.yaml:44-46` darwin codesign post-hook. **WIRED.**
5. **E2E HA pause/resume golden path (Phase 58)** — CLI flag → ResumeOptions → lifecycle dispatch → Phase 1.25 LB reapply → Phase 1.5 ip-pin OR Phase 4.5 cert-regen widened. `hack/uat-47-ha-smoke.sh` references `${REPO_ROOT}/bin/kinder` (no PATH lookups). Live log ends `=== ALL TESTS PASSED ===` on HEAD `e0ec855e`. **WIRED.**

### End-to-end flows verified

- **HA pause/resume** — both ip-pin and cert-regen paths chain CLI → lifecycle dispatch → Phase 1.25 LB reapply with cluster-authoritative label.
- **Addon install** — 14 pinned images consumed by `offlinereadiness` check loop.
- **PR CI** — `build-check.yml` blocks on windows cross-compile failure.
- **macOS release** — `builds[0].hooks.post` codesigns darwin inline; verify workflow gates on signature.
- **Doctor 26 checks under `-race`** — race gate enforced by Makefile + workflow.

**No cross-phase breakage detected. Zero BLOCKER findings.**

### Advisory warnings (non-blocking)

1. **`build-check.yml` not yet a required status check** on `main` — Workflow runs on every PR but doesn't gate merge. Repo-admin setting; not a code gap.
2. **`macos-sign-verify.yml` post-merge only** — Triggers on `push: branches: [main]`, not on PRs. Matches design (drift detection), not pre-merge gating.

---

## Tech Debt (non-blocking; filed for v2.5)

1. **DIAG-06 wording** — `resumereadiness.go` emits `etcd endpoint health probe failed` instead of SC2-specified `quorum at risk`. Behavior correct; wording cosmetic. Tracked.
2. **`nerdctl.lima` basename fragility** — IPAM probe `VerdictUnsupported` short-circuit (`binary == "nerdctl"`) does not match Lima wrapper basename. Masked today by error-path fall-through. Low severity.
3. **UAT CI wiring** — 4 UAT shell scripts (`hack/uat-{47-ha-smoke,51-envoy-ipvs-guide,57.3-cert-regen,57.4-ipam-probe}.sh`) are manual-only. No self-hosted-runner / matrix OS wiring. Deferred to v2.5.
4. **`./bin/kinder version` git hash** — `versionPreRelease` suppresses hash. Strings-marker gate used as Phase 58 workaround. Track for v2.5 cosmetic phase.
5. **Branch protection on Windows build** — `build-check.yml` not yet a required check on `main`. Repo-admin task.

---

## Deferred (correctly carried forward)

| Requirement | Reason | Re-trigger condition |
|---|---|---|
| **SYNC-05** | `kindest/node:v1.36.x` not on Docker Hub (Plan 53-00 Outcome B, 2026-05-10). `pkg/apis/config/defaults/image.go` correctly remains at v1.35.1. | Re-run Plan 53-00 when kind v0.32.0 publishes the v1.36 image. Plan 51-04 Task 2 is fully authored and ready to execute atomically. |

---

## Verdict

**v2.4 Hardening is feature-complete, architecturally clean, and ready to ship.**

- 13/13 active requirements **SATISFIED**; 1 correctly **DEFERRED** (external blocker).
- 5/5 cross-phase wiring chains confirmed by independent integration check.
- 5/5 end-to-end flows traceable end-to-end.
- 2 live UAT runs (`hack/uat-47-ha-smoke.log`, `hack/uat-51-envoy-ipvs-guide.log`) committed as canonical evidence.
- 11/11 phases verified via `*-VERIFICATION.md`.
- 0 critical blockers; 5 non-blocking tech debt items filed for v2.5.

REQUIREMENTS.md, PROJECT.md, and ROADMAP.md are reconciled — all 13 active requirements show `[x] Complete` and SYNC-05 is correctly marked `[ ] DEFERRED` with external-blocker rationale. No documentation drift remaining.

Routing to `/gsd:complete-milestone v2.4`.

---

_Audited: 2026-05-31_
_Auditor: Claude (gsd:audit-milestone v2.4)_
_Integration check: gsd-integration-checker — 5/5 wiring, 5/5 flows, 0 blockers, 2 advisory warnings_
