# Requirements Archive: v2.4 Hardening

**Archived:** 2026-05-31
**Status:** SHIPPED (with one external-blocker deferral) — released as public tag `v1.6`

For current requirements, see `.planning/REQUIREMENTS.md` (created fresh for the next milestone).

---

# Requirements: Kinder v2.4 Hardening

**Defined:** 2026-05-09
**Core Value:** A single command gives developers a local Kubernetes cluster where LoadBalancer services, Gateway API routing, metrics, and dashboards all work without any manual setup.

## v2.4 Requirements

### Lifecycle

- [x] **LIFE-09**: HA pause/resume preserves etcd peer connectivity across container IP reassignment via IP pinning (`docker network connect --ip <stored-ip>`); cert regen is documented fallback if Docker IPAM is infeasible
  - **Phase 57.3 scope append**: cert-regen fallback path widened beyond `etcd-peer` to all four etcd-adjacent certs (`etcd-peer`, `etcd-server`, `etcd-healthcheck-client`, `apiserver-etcd-client`); 20s static sleep replaced by active `etcdctl` + `curl` health-gates (1s tick, 60s deadline); post-pass `kubeadm certs check-expiration` verify; `--strategy=<auto|ip-pin|cert-regen>` flag; IPv6/dual-stack loopback addressing derived from cluster ip-family label
  - **Phase 57.4 scope append**: `--privileged` added to IPAM probe container start (`ipamprobe.go:111`); closes the macOS Docker Desktop 4.73.0 regression where `NetworkSettings.Networks` was empty for non-privileged containers, causing `net.ParseIP` to reject the empty string and route every HA cluster to the cert-regen fallback
  - **Outcome:** SATISFIED — Phases 52 + 57.1 + 57.2 + 57.3 + 57.4; live-UAT'd across IPv4/IPv6/dual

### Addons

- [x] **ADDON-01**: `local-path-provisioner` bumped to v0.0.36 (CVE GHSA-7fxv-8wr2-mfc4 HelperPod Template Injection security fix); embedded busybox pin retained — SATISFIED
- [x] **ADDON-02**: `Headlamp` bumped to v0.42.0 with mandatory plan-time token-print flow verification against a real cluster — SATISFIED (Path A; live UAT-2)
- [x] **ADDON-03**: `cert-manager` bumped to v1.20.2 with self-signed ClusterIssuer smoke covering UID change (1000→65532); `--server-side` apply preserved (989 KB > 256 KB annotation limit) — SATISFIED (UID enforced via distroless USER + runAsNonRoot, not manifest runAsUser)
- [x] **ADDON-04**: `Envoy Gateway` bumped to v1.7.2 (two major lines) with dedicated HTTPRoute UAT; Gateway API CRDs v1.2.1 → v1.4.1 in-band — SATISFIED (HTTP 200 end-to-end)
- [x] **ADDON-05**: All bumped image references propagated to `offlinereadiness.go` `allAddonImages`; `TestAllAddonImages_CountMatchesExpected` updated (count=14) — SATISFIED

### Upstream Sync

- [ ] **SYNC-05** *(DEFERRED — external blocker)*: Default `kindest/node` bumped to K8s 1.36.x conditional on Docker Hub publication. Plan 53-00 ran Outcome B (count=0) on 2026-05-10 — `kindest/node:v1.36.x` not on Docker Hub. `pkg/apis/config/defaults/image.go` correctly remains at v1.35.1. Re-run when kind publishes v0.32.0 + the v1.36 image. Plan 51-04 Task 2 is fully authored and ready to re-execute atomically.

### Distribution

- [x] **DIST-01**: macOS GoReleaser artifacts ad-hoc signed via `codesign --force --sign -` post-hook on `macos-latest`; signing happens AFTER `-ldflags="-s -w"` strip; release notes state ad-hoc signing does NOT bypass Gatekeeper quarantine — SATISFIED (CI run 25746519788)
- [x] **DIST-02**: PR CI gains a blocking `GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build ./...` step; failure fails the PR check — SATISFIED (workflow-level; CI run 25750801764; merge-level branch protection deferred)

### Tech Debt

- [x] **DEBT-04**: Pre-existing data race in `pkg/internal/doctor` (`allChecks` global mutated under `t.Parallel()`) eliminated; production read path remains lock-free; tests use scoped helper `runChecks(checks []Check)` — SATISFIED (zero DATA RACE over 100 iterations)

### Diagnostics

- [x] **DIAG-05**: `cluster-node-skew` doctor check skips containers with role `external-load-balancer` / `external-etcd` — SATISFIED (inline role guard in realListNodes)
- [x] **DIAG-06**: `cluster-resume-readiness` parses `etcdctl endpoint health --write-out json` and reports actionable reason text instead of raw error — SATISFIED (behavior correct; exact wording divergence filed for v2.5)

### User Acceptance

- [x] **UAT-01**: Phase 47 live HA UAT closed — pause/resume worker→CP→LB ordering, host resource observation, cluster-state round-trip; rebuilt binary verified before run — SATISFIED (8/8; 47-UAT.md 14/14)
- [x] **UAT-02**: Phase 51 live UAT closed — `envoyproxy/envoy` (not `kindest/haproxy`) on real HA cluster; IPVS+1.36 rejected at validate; K8s 1.36 guide renders — SATISFIED (3/3; 51-UAT.md augmented)

## Documented Holds

| Addon | Pin | Rationale |
|-------|-----|-----------|
| MetalLB | v0.15.3 | Latest available; no v0.16 released (verified 2026-05-10) |
| Metrics Server | v0.8.1 | Latest stable as of 2026-01-29; no v0.9.x |
| registry (local registry) | registry:2 (2.8.3) | v3 deprecated storage drivers; kind ecosystem on v2 |
| CoreDNS | (in-cluster) | Tuned at runtime; no version pin |

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| LIFE-09 | 52 (+57.1/57.2/57.3/57.4) | Complete |
| ADDON-01 | 53 | Complete |
| ADDON-02 | 53 | Complete |
| ADDON-03 | 53 | Complete |
| ADDON-04 | 53 | Complete |
| ADDON-05 | 53 | Complete |
| SYNC-05 | 53 | Deferred (external blocker) |
| DIST-01 | 54 | Complete |
| DIST-02 | 55 | Complete |
| DEBT-04 | 56 | Complete |
| DIAG-05 | 57 | Complete |
| DIAG-06 | 57 | Complete |
| UAT-01 | 58 | Complete |
| UAT-02 | 58 | Complete |

---

## Milestone Summary

**Shipped:** 13 of 14 requirements (released as public tag `v1.6`)
**Deferred:** 1 — SYNC-05 (external blocker: `kindest/node:v1.36.x` not on Docker Hub; re-run when kind v0.32.0 publishes the image)
**Adjusted in flight:** LIFE-09 grew two scope appends (57.3 cert-regen widening, 57.4 IPAM `--privileged`) as live UAT surfaced stacked upstream defects; four decimal phases (57.1–57.4) were inserted under it
**Dropped:** none

**Carried forward to v2.5 (from REQUIREMENTS.md "v2.5+" section):**
- LIFE-10/11/12 — pause/resume + dev parity for podman and nerdctl providers
- SNAP-01 — snapshot remote-storage backend (S3/GCS/registry)
- DIST-03 — full macOS notarization (Apple Developer cert + notarytool + staple)
- DIAG-07 — etcd peer-TLS regen via `kubeadm certs renew etcd-peer` escape hatch
- SYNC-05 — default node image bump (external blocker)

---
*Archived: 2026-05-31 as part of v2.4 milestone completion (released as v1.6)*
